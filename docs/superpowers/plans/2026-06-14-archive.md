# Архив прошлых рабочих дней Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Реализовать вкладку `Архив` для прошлых локальных рабочих дней: поиск, фильтры дат, просмотр, редактирование итогов, восстановление и повторная обработка.

**Architecture:** Добавить небольшой слой архивных моделей и поиска поверх существующего `StorageService`, не меняя структуру локальных папок. UI остается в `MainWindow`, но архивное состояние, поиск и чтение данных должны быть отделены от текущего `Ревью` и не подменять `active_day_folder`.

**Tech Stack:** Python 3.11+, PySide6, pytest, локальная файловая структура `MeetingSummaries/YYYY-MM-DD`, существующие `StorageService`, meeting pipeline и day summary pipeline.

---

## Контекст и границы

Репозиторий: `Damidamida/apps-meeting-day-recorder`.

Базовая ветка для реализации: актуальная `main`. Рабочая ветка должна быть `codex/archive`.

Текущий этап проекта: этап 8 `UX/safety polish` остается `На проверке`; этап 9 не начат. После реализации статус этапа не переводить в `Готово`.

Явно исключено:

- сегодняшний день в Архиве;
- редактирование transcript;
- календарная визуализация месяца;
- отдельные режимы `Черновик` / `Финал`;
- отдельное подтверждение перед внешними AI-запросами;
- изменение структуры локальных папок;
- пуш или merge в `main`.

Обязательные файлы перед реализацией каждой задачи:

- `AGENTS.md`;
- `PROJECT_STATE.md`;
- `docs/superpowers/specs/2026-06-14-archive-design.md`;
- этот план.

## Карта файлов

Создать:

- `app/services/archive.py` — архивные dataclass-модели, фильтрация дней, поиск по локальным файлам.
- `tests/test_archive.py` — unit-тесты архивного слоя и поиска.

Изменить:

- `app/services/storage.py` — небольшие методы чтения прошлых дней и сохранения `summary_final.md` встречи.
- `app/ui/main_window.py` — полноценная вкладка `Архив`, UI-state, события, действия.
- `tests/test_storage.py` — тесты новых методов `StorageService`.
- `tests/test_ui.py` — UI-тесты Архива.
- `PROJECT_STATE.md` — статус, выполненная работа, следующий шаг, тесты.

Не менять:

- формат папок `MeetingSummaries/YYYY-MM-DD`;
- lifecycle сегодняшнего дня;
- OBS/FFmpeg/Whisper/Summary backend-реализации;
- `config.yaml` и `.env`.

---

### Task 1: Storage APIs для прошлых дней и финала встречи

**Files:**
- Modify: `app/services/storage.py`
- Test: `tests/test_storage.py`

- [ ] **Step 1: Write failing storage tests**

Add tests near existing workday folder tests:

```python
def test_list_past_workday_folders_excludes_today_and_sorts_newest_first(tmp_path) -> None:
    storage = StorageService(tmp_path)
    today = datetime(2026, 6, 14, 12, 0)
    old_day = storage.create_day_folder(date(2026, 6, 10))
    recent_day = storage.create_day_folder(date(2026, 6, 13))
    today_day = storage.create_day_folder(date(2026, 6, 14))
    storage._write_json(old_day / "day_metadata.json", {"date": "2026-06-10", "status": "ended"})
    storage._write_json(recent_day / "day_metadata.json", {"date": "2026-06-13", "status": "ended"})
    storage._write_json(today_day / "day_metadata.json", {"date": "2026-06-14", "status": "active"})

    assert storage.list_past_workday_folders(today) == [recent_day, old_day]


def test_list_past_workday_folders_skips_non_workday_folders(tmp_path) -> None:
    storage = StorageService(tmp_path)
    (tmp_path / "notes").mkdir()
    day_folder = storage.create_day_folder(date(2026, 6, 12))
    storage._write_json(day_folder / "day_metadata.json", {"date": "2026-06-12", "status": "ended"})

    assert storage.list_past_workday_folders(datetime(2026, 6, 14, 12, 0)) == [day_folder]


def test_save_meeting_summary_final_writes_final_without_touching_draft(tmp_path) -> None:
    storage = StorageService(tmp_path)
    meeting_folder = storage.create_meeting_folder("Архив", datetime(2026, 6, 12, 9, 0))
    storage.save_meeting_summary_draft(meeting_folder, "# Черновик\n")

    final_path = storage.save_meeting_summary_final(meeting_folder, "# Финал\n")

    assert final_path == meeting_folder / "summary_final.md"
    assert (meeting_folder / "summary_draft.md").read_text(encoding="utf-8") == "# Черновик\n"
    assert final_path.read_text(encoding="utf-8") == "# Финал\n"
```

- [ ] **Step 2: Run failing tests**

Run:

```powershell
python -m pytest tests/test_storage.py::test_list_past_workday_folders_excludes_today_and_sorts_newest_first tests/test_storage.py::test_list_past_workday_folders_skips_non_workday_folders tests/test_storage.py::test_save_meeting_summary_final_writes_final_without_touching_draft -q
```

Expected: fail because `list_past_workday_folders` and `save_meeting_summary_final` do not exist.

- [ ] **Step 3: Implement minimal StorageService methods**

In `app/services/storage.py`, add:

```python
    def list_past_workday_folders(self, now: datetime | None = None) -> list[Path]:
        now = now or datetime.now()
        today = now.date()
        candidates: list[tuple[date, Path]] = []
        if not self.root.is_dir():
            return []
        for day_folder in self.root.iterdir():
            if not day_folder.is_dir():
                continue
            metadata_path = day_folder / "day_metadata.json"
            if not metadata_path.is_file():
                continue
            try:
                metadata = self._read_json(metadata_path)
                workday = date.fromisoformat(str(metadata.get("date") or day_folder.name))
            except (MetadataReadError, ValueError):
                continue
            if workday < today:
                candidates.append((workday, day_folder))
        return [day_folder for _, day_folder in sorted(candidates, reverse=True)]

    def save_meeting_summary_final(self, meeting_folder: Path, content: str) -> Path:
        return self._write_text(Path(meeting_folder) / "summary_final.md", content)
```

Place `list_past_workday_folders` near `find_past_active_workday`, and `save_meeting_summary_final` near `save_meeting_summary_draft`.

- [ ] **Step 4: Run storage tests**

Run:

```powershell
python -m pytest tests/test_storage.py::test_list_past_workday_folders_excludes_today_and_sorts_newest_first tests/test_storage.py::test_list_past_workday_folders_skips_non_workday_folders tests/test_storage.py::test_save_meeting_summary_final_writes_final_without_touching_draft -q
```

Expected: `3 passed`.

- [ ] **Step 5: Commit**

```powershell
git add app/services/storage.py tests/test_storage.py
git commit -m "Добавить методы хранения для Архива"
```

---

### Task 2: Архивные модели и локальный поиск

**Files:**
- Create: `app/services/archive.py`
- Create: `tests/test_archive.py`

- [ ] **Step 1: Write failing archive service tests**

Create `tests/test_archive.py`:

```python
from datetime import date, datetime

from app.services.archive import ArchiveDateFilter, build_archive_days, search_archive
from app.services.storage import StorageService


def test_build_archive_days_returns_statuses_and_meetings(tmp_path) -> None:
    storage = StorageService(tmp_path)
    day_folder = storage.create_day_folder(date(2026, 6, 12))
    storage._write_json(day_folder / "day_metadata.json", {"date": "2026-06-12", "status": "ended"})
    meeting = storage.create_meeting_folder("Планирование релиза", datetime(2026, 6, 12, 9, 30))
    storage.save_day_summary_draft(day_folder, "# Итоги дня\n")
    metadata = storage.read_meeting_metadata(meeting)
    metadata.update({"status": "ended", "processing_status": "completed"})
    storage.write_metadata(meeting, metadata)

    days = build_archive_days(storage, now=datetime(2026, 6, 14, 12, 0))

    assert len(days) == 1
    assert days[0].folder == day_folder
    assert days[0].workday == date(2026, 6, 12)
    assert days[0].meeting_count == 1
    assert days[0].status_label == "Итоги готовы"
    assert days[0].meetings[0].title == "Планирование релиза"


def test_archive_date_filter_limits_days(tmp_path) -> None:
    storage = StorageService(tmp_path)
    old_day = storage.create_day_folder(date(2026, 5, 20))
    recent_day = storage.create_day_folder(date(2026, 6, 12))
    storage._write_json(old_day / "day_metadata.json", {"date": "2026-05-20", "status": "ended"})
    storage._write_json(recent_day / "day_metadata.json", {"date": "2026-06-12", "status": "ended"})

    days = build_archive_days(
        storage,
        now=datetime(2026, 6, 14, 12, 0),
        date_filter=ArchiveDateFilter(start=date(2026, 6, 1), end=date(2026, 6, 14)),
    )

    assert [day.folder for day in days] == [recent_day]


def test_search_archive_finds_title_summary_day_summary_and_transcript(tmp_path) -> None:
    storage = StorageService(tmp_path)
    day_folder = storage.create_day_folder(date(2026, 6, 12))
    storage._write_json(day_folder / "day_metadata.json", {"date": "2026-06-12", "status": "ended"})
    storage.save_day_summary_draft(day_folder, "Общие итоги про релиз")
    meeting = storage.create_meeting_folder("Планирование", datetime(2026, 6, 12, 9, 30))
    storage.save_meeting_summary_draft(meeting, "Риски релиза")
    (meeting / "transcript.md").write_text("Обсудили релиз и метрики", encoding="utf-8")

    days = build_archive_days(storage, now=datetime(2026, 6, 14, 12, 0))
    matches = search_archive(days, "релиз")

    assert {match.kind for match in matches} >= {
        "Итоги дня",
        "Итоги встречи",
        "Транскрипт",
    }
    assert all(match.day_folder == day_folder for match in matches)
```

- [ ] **Step 2: Run failing archive tests**

Run:

```powershell
python -m pytest tests/test_archive.py -q
```

Expected: fail because `app.services.archive` does not exist.

- [ ] **Step 3: Implement archive models and functions**

Create `app/services/archive.py`:

```python
from __future__ import annotations

from dataclasses import dataclass, field
from datetime import date, datetime, timedelta
from pathlib import Path

from app.services.storage import MetadataReadError, StorageService


@dataclass(frozen=True)
class ArchiveDateFilter:
    start: date | None = None
    end: date | None = None

    def includes(self, workday: date) -> bool:
        if self.start is not None and workday < self.start:
            return False
        if self.end is not None and workday > self.end:
            return False
        return True

    @classmethod
    def week(cls, now: datetime) -> "ArchiveDateFilter":
        return cls(start=now.date() - timedelta(days=7), end=now.date())

    @classmethod
    def month(cls, now: datetime) -> "ArchiveDateFilter":
        return cls(start=now.date() - timedelta(days=31), end=now.date())


@dataclass(frozen=True)
class ArchiveMeeting:
    folder: Path
    day_folder: Path
    title: str
    started_at: str
    status_label: str
    metadata: dict[str, object] = field(default_factory=dict)


@dataclass(frozen=True)
class ArchiveDay:
    folder: Path
    workday: date
    status_label: str
    detail_text: str
    meeting_count: int
    has_day_summary: bool
    has_unfinished_processing: bool
    metadata: dict[str, object] = field(default_factory=dict)
    meetings: list[ArchiveMeeting] = field(default_factory=list)


@dataclass(frozen=True)
class ArchiveSearchMatch:
    kind: str
    day_folder: Path
    meeting_folder: Path | None
    title: str
    snippet: str


def build_archive_days(
    storage: StorageService,
    now: datetime | None = None,
    date_filter: ArchiveDateFilter | None = None,
) -> list[ArchiveDay]:
    now = now or datetime.now()
    days: list[ArchiveDay] = []
    for day_folder in storage.list_past_workday_folders(now):
        try:
            metadata = storage.read_day_metadata(day_folder)
            workday = date.fromisoformat(str(metadata.get("date") or day_folder.name))
        except (MetadataReadError, ValueError):
            continue
        if date_filter is not None and not date_filter.includes(workday):
            continue
        meetings = _archive_meetings(storage, day_folder)
        has_summary = storage.day_summary_exists(day_folder)
        has_unfinished = storage.has_unfinished_meeting_processing(day_folder)
        status_label = _day_status_label(storage, day_folder, metadata, has_summary, has_unfinished)
        detail_text = f"{len(meetings)} встреч"
        days.append(
            ArchiveDay(
                folder=day_folder,
                workday=workday,
                status_label=status_label,
                detail_text=detail_text,
                meeting_count=len(meetings),
                has_day_summary=has_summary,
                has_unfinished_processing=has_unfinished,
                metadata=metadata,
                meetings=meetings,
            )
        )
    return days


def search_archive(days: list[ArchiveDay], query: str) -> list[ArchiveSearchMatch]:
    normalized = query.casefold().strip()
    if not normalized:
        return []
    matches: list[ArchiveSearchMatch] = []
    for archive_day in days:
        day_text = archive_day.workday.isoformat()
        if normalized in day_text.casefold():
            matches.append(
                ArchiveSearchMatch("Дата", archive_day.folder, None, day_text, day_text)
            )
        _append_file_match(
            matches,
            "Итоги дня",
            archive_day.folder,
            None,
            "Итоги дня",
            archive_day.folder / "00_day_summary_draft.md",
            normalized,
        )
        _append_file_match(
            matches,
            "Итоги дня",
            archive_day.folder,
            None,
            "Итоги дня",
            archive_day.folder / "00_day_summary_final.md",
            normalized,
        )
        for meeting in archive_day.meetings:
            if normalized in meeting.title.casefold():
                matches.append(
                    ArchiveSearchMatch(
                        "Название встречи",
                        archive_day.folder,
                        meeting.folder,
                        meeting.title,
                        meeting.title,
                    )
                )
            _append_file_match(
                matches,
                "Итоги встречи",
                archive_day.folder,
                meeting.folder,
                meeting.title,
                meeting.folder / "summary_draft.md",
                normalized,
            )
            _append_file_match(
                matches,
                "Итоги встречи",
                archive_day.folder,
                meeting.folder,
                meeting.title,
                meeting.folder / "summary_final.md",
                normalized,
            )
            _append_file_match(
                matches,
                "Транскрипт",
                archive_day.folder,
                meeting.folder,
                meeting.title,
                meeting.folder / "transcript.md",
                normalized,
            )
    return matches


def _archive_meetings(storage: StorageService, day_folder: Path) -> list[ArchiveMeeting]:
    meetings: list[ArchiveMeeting] = []
    for meeting_folder in sorted(
        storage.list_meeting_folders(day_folder),
        key=lambda folder: _meeting_sort_key(storage, folder),
        reverse=True,
    ):
        try:
            metadata = storage.read_meeting_metadata(meeting_folder)
        except MetadataReadError:
            meetings.append(
                ArchiveMeeting(meeting_folder, day_folder, meeting_folder.name, "", "Требует внимания")
            )
            continue
        meetings.append(
            ArchiveMeeting(
                folder=meeting_folder,
                day_folder=day_folder,
                title=str(metadata.get("title") or meeting_folder.name),
                started_at=str(metadata.get("started_at") or ""),
                status_label=_meeting_status_label(metadata),
                metadata=metadata,
            )
        )
    return meetings


def _meeting_sort_key(storage: StorageService, meeting_folder: Path) -> tuple[str, str]:
    try:
        metadata = storage.read_meeting_metadata(meeting_folder)
    except MetadataReadError:
        return "", meeting_folder.name
    return str(metadata.get("started_at") or ""), meeting_folder.name


def _day_status_label(
    storage: StorageService,
    day_folder: Path,
    metadata: dict[str, object],
    has_summary: bool,
    has_unfinished: bool,
) -> str:
    if metadata.get("status") == "active":
        return "Незавершен"
    if has_unfinished:
        return "Обработка дня"
    try:
        day_summary = storage.read_day_summary_metadata(day_folder)
    except MetadataReadError:
        return "Требует внимания"
    status = day_summary.get("day_summary_status")
    if status in {"draft_created", "up_to_date"} or has_summary:
        return "Итоги готовы"
    if status in {"failed", "openai_unavailable"}:
        return "Требует внимания"
    return "В очереди"


def _meeting_status_label(metadata: dict[str, object]) -> str:
    processing_status = metadata.get("processing_status")
    if metadata.get("status") == "active":
        return "Активна"
    if processing_status in {"pending", "running"}:
        return "В очереди"
    if processing_status == "failed":
        return "Требует внимания"
    if metadata.get("summary_status") == "draft_created":
        return "Итоги готовы"
    return "Без итогов"


def _append_file_match(
    matches: list[ArchiveSearchMatch],
    kind: str,
    day_folder: Path,
    meeting_folder: Path | None,
    title: str,
    path: Path,
    normalized_query: str,
) -> None:
    if not path.is_file():
        return
    try:
        text = path.read_text(encoding="utf-8")
    except OSError:
        return
    index = text.casefold().find(normalized_query)
    if index < 0:
        return
    start = max(0, index - 40)
    end = min(len(text), index + len(normalized_query) + 40)
    snippet = text[start:end].replace("\n", " ").strip()
    matches.append(ArchiveSearchMatch(kind, day_folder, meeting_folder, title, snippet))
```

- [ ] **Step 4: Run archive tests**

Run:

```powershell
python -m pytest tests/test_archive.py -q
```

Expected: `3 passed`.

- [ ] **Step 5: Commit**

```powershell
git add app/services/archive.py tests/test_archive.py
git commit -m "Добавить модель и поиск Архива"
```

---

### Task 3: UI-скелет Архива, empty-state и список дней

**Files:**
- Modify: `app/ui/main_window.py`
- Test: `tests/test_ui.py`

- [ ] **Step 1: Write failing UI tests for archive shell**

Replace `test_archive_and_help_pages_explain_placeholders_and_local_flow` with focused assertions for the new Archive shell, and keep help assertions in a separate test if needed:

```python
def test_archive_page_shows_past_days_and_excludes_today(tmp_path: Path) -> None:
    app = QApplication.instance() or QApplication([])
    recorder = NoopRecorder()
    storage = StorageService(tmp_path, recorder)
    today = datetime.now()
    past_day = storage.create_day_folder((today - timedelta(days=1)).date())
    today_day = storage.create_day_folder(today.date())
    storage._write_json(past_day / "day_metadata.json", {"date": (today - timedelta(days=1)).date().isoformat(), "status": "ended"})
    storage._write_json(today_day / "day_metadata.json", {"date": today.date().isoformat(), "status": "active"})

    window = MainWindow(storage, recorder)
    window.nav_buttons[2].click()
    archive_text = "\n".join(label.text() for label in window.pages.widget(2).findChildren(QLabel))

    assert "Прошлые дни" in archive_text
    assert past_day.name in archive_text
    assert today_day.name not in archive_text

    window.close()
    app.processEvents()


def test_archive_page_empty_state_explains_today_location(tmp_path: Path) -> None:
    app = QApplication.instance() or QApplication([])
    recorder = NoopRecorder()
    storage = StorageService(tmp_path, recorder)

    window = MainWindow(storage, recorder)
    window.nav_buttons[2].click()
    archive_text = "\n".join(label.text() for label in window.pages.widget(2).findChildren(QLabel))

    assert "Прошлых рабочих дней пока нет" in archive_text
    assert "сегодняшний день находится во вкладке `Рабочий день`" in archive_text

    window.close()
    app.processEvents()
```

- [ ] **Step 2: Run failing UI tests**

Run:

```powershell
python -m pytest tests/test_ui.py::test_archive_page_shows_past_days_and_excludes_today tests/test_ui.py::test_archive_page_empty_state_explains_today_location -q
```

Expected: fail because Archive still shows placeholder text.

- [ ] **Step 3: Add Archive imports and state**

In `app/ui/main_window.py`, add imports:

```python
from app.services.archive import ArchiveDateFilter, ArchiveDay, ArchiveSearchMatch, build_archive_days, search_archive
```

In `MainWindow.__init__`, near review state fields, add:

```python
        self.archive_days: list[ArchiveDay] = []
        self.archive_matches: list[ArchiveSearchMatch] = []
        self.selected_archive_day_folder: Path | None = None
        self.selected_archive_meeting_folder: Path | None = None
        self.archive_selected_material: str = "day_summary"
        self.archive_date_filter: ArchiveDateFilter | None = None
        self.archive_query = ""
```

- [ ] **Step 4: Replace `_create_archive_page` with shell widgets**

Implement:

```python
    def _create_archive_page(self) -> QWidget:
        page = QWidget()
        self._prepare_page_surface(page)
        layout = QVBoxLayout()
        layout.setContentsMargins(0, 0, 0, 0)
        layout.setSpacing(14)
        layout.addWidget(
            self._create_page_header(
                "Архив",
                "Прошлые рабочие дни и встречи. Сегодняшний день остается во вкладке `Рабочий день`.",
            )
        )

        self.archive_search_input = QLineEdit()
        self.archive_search_input.setPlaceholderText("Поиск по дате, названию, итогам или transcript")
        self.archive_search_input.textChanged.connect(self._on_archive_search_changed)
        layout.addWidget(self.archive_search_input)

        content_splitter = QSplitter(Qt.Orientation.Horizontal)
        left_panel = QWidget()
        left_layout = QVBoxLayout()
        left_layout.setContentsMargins(0, 0, 0, 0)
        left_layout.setSpacing(10)
        left_layout.addWidget(QLabel("Прошлые дни"))
        self.archive_days_layout = QVBoxLayout()
        self.archive_days_layout.setSpacing(8)
        left_layout.addLayout(self.archive_days_layout)
        left_layout.addStretch(1)
        left_panel.setLayout(left_layout)

        right_panel = QWidget()
        self.archive_detail_layout = QVBoxLayout()
        self.archive_detail_layout.setContentsMargins(0, 0, 0, 0)
        self.archive_detail_layout.setSpacing(10)
        right_panel.setLayout(self.archive_detail_layout)

        content_splitter.addWidget(left_panel)
        content_splitter.addWidget(right_panel)
        content_splitter.setStretchFactor(0, 0)
        content_splitter.setStretchFactor(1, 1)
        layout.addWidget(content_splitter, 1)
        page.setLayout(layout)
        return page
```

- [ ] **Step 5: Add refresh methods and call them**

Add:

```python
    def refresh_archive(self) -> None:
        self.archive_days = build_archive_days(
            self.storage,
            date_filter=self.archive_date_filter,
        )
        self.archive_matches = search_archive(self.archive_days, self.archive_query)
        visible_days = self._visible_archive_days()
        if self.selected_archive_day_folder not in {day.folder for day in visible_days}:
            self.selected_archive_day_folder = visible_days[0].folder if visible_days else None
            self.selected_archive_meeting_folder = None
            self.archive_selected_material = "day_summary"
        self._refresh_archive_days()
        self._refresh_archive_detail()

    def _on_archive_search_changed(self, text: str) -> None:
        self.archive_query = text.strip()
        self.refresh_archive()

    def _visible_archive_days(self) -> list[ArchiveDay]:
        if not self.archive_query:
            return self.archive_days
        day_folders = {match.day_folder for match in self.archive_matches}
        return [day for day in self.archive_days if day.folder in day_folders]
```

Update nav button for Archive to call a new method:

```python
    def open_archive(self) -> None:
        self.pages.setCurrentIndex(2)
        self.refresh_archive()
```

and change archive nav connection from `lambda: self.pages.setCurrentIndex(2)` to `self.open_archive`.

- [ ] **Step 6: Render day cards and detail placeholder**

Add:

```python
    def _refresh_archive_days(self) -> None:
        self._clear_layout(self.archive_days_layout)
        visible_days = self._visible_archive_days()
        if not visible_days:
            empty = QLabel(
                "Прошлых рабочих дней пока нет. Сегодняшний день находится во вкладке `Рабочий день`."
            )
            empty.setObjectName("emptyState")
            empty.setWordWrap(True)
            self.archive_days_layout.addWidget(empty)
            return
        for day in visible_days:
            self.archive_days_layout.addWidget(self._create_archive_day_card(day))

    def _create_archive_day_card(self, archive_day: ArchiveDay) -> QWidget:
        selected = archive_day.folder == self.selected_archive_day_folder
        card = ClickableFrame()
        card.setObjectName("activeMeetingCard" if selected else "meetingCard")
        card.setAttribute(Qt.WidgetAttribute.WA_StyledBackground, True)
        card.clicked.connect(lambda folder=archive_day.folder: self.select_archive_day(folder))
        layout = QVBoxLayout()
        layout.setContentsMargins(12, 10, 12, 10)
        layout.setSpacing(6)
        header = QLabel(archive_day.workday.isoformat())
        header.setObjectName("meetingHeaderLabel")
        header.setAttribute(Qt.WidgetAttribute.WA_TransparentForMouseEvents, True)
        detail = QLabel(f"{archive_day.meeting_count} встреч · {archive_day.status_label}")
        detail.setObjectName("sectionHint")
        detail.setWordWrap(True)
        detail.setAttribute(Qt.WidgetAttribute.WA_TransparentForMouseEvents, True)
        layout.addWidget(header)
        layout.addWidget(detail)
        card.setLayout(layout)
        return card

    def select_archive_day(self, day_folder: Path) -> None:
        self.selected_archive_day_folder = day_folder
        self.selected_archive_meeting_folder = None
        self.archive_selected_material = "day_summary"
        self.refresh_archive()

    def _refresh_archive_detail(self) -> None:
        self._clear_layout(self.archive_detail_layout)
        day = self._selected_archive_day()
        if day is None:
            self.archive_detail_layout.addWidget(QLabel("Выберите прошлый рабочий день."))
            return
        self.archive_detail_layout.addWidget(self._create_card("Итоги дня", self._archive_day_summary_layout(day)))
        meetings_label = QLabel("Встречи дня")
        meetings_label.setObjectName("sectionTitle")
        self.archive_detail_layout.addWidget(meetings_label)
        for meeting in day.meetings:
            self.archive_detail_layout.addWidget(QLabel(meeting.title))
        self.archive_detail_layout.addStretch(1)

    def _selected_archive_day(self) -> ArchiveDay | None:
        for day in self.archive_days:
            if day.folder == self.selected_archive_day_folder:
                return day
        return None

    def _archive_day_summary_layout(self, day: ArchiveDay) -> QVBoxLayout:
        layout = QVBoxLayout()
        label = QLabel("Итоги дня")
        label.setWordWrap(True)
        layout.addWidget(label)
        return layout
```

- [ ] **Step 7: Run UI tests**

Run:

```powershell
python -m pytest tests/test_ui.py::test_archive_page_shows_past_days_and_excludes_today tests/test_ui.py::test_archive_page_empty_state_explains_today_location -q
```

Expected: `2 passed`.

- [ ] **Step 8: Commit**

```powershell
git add app/ui/main_window.py tests/test_ui.py
git commit -m "Добавить базовый экран Архива"
```

---

### Task 4: Фильтры дат и компактные результаты поиска

**Files:**
- Modify: `app/ui/main_window.py`
- Test: `tests/test_ui.py`

- [ ] **Step 1: Write failing tests for filters and search UI**

Add:

```python
def test_archive_search_filters_days_and_shows_compact_matches(tmp_path: Path) -> None:
    app = QApplication.instance() or QApplication([])
    recorder = NoopRecorder()
    storage = StorageService(tmp_path, recorder)
    day_folder = storage.create_day_folder((datetime.now() - timedelta(days=1)).date())
    storage._write_json(day_folder / "day_metadata.json", {"date": day_folder.name, "status": "ended"})
    meeting = storage.create_meeting_folder("Планирование релиза", datetime.fromisoformat(f"{day_folder.name}T09:30:00"))
    storage.save_meeting_summary_draft(meeting, "Риски релиза")

    window = MainWindow(storage, recorder)
    window.open_archive()
    window.archive_search_input.setText("релиз")
    app.processEvents()
    archive_text = "\n".join(label.text() for label in window.pages.widget(2).findChildren(QLabel))

    assert "Найдено:" in archive_text
    assert "Планирование релиза" in archive_text
    assert "Итоги встречи" in archive_text

    window.close()
    app.processEvents()


def test_archive_week_filter_hides_older_days(tmp_path: Path) -> None:
    app = QApplication.instance() or QApplication([])
    recorder = NoopRecorder()
    storage = StorageService(tmp_path, recorder)
    recent_day = storage.create_day_folder((datetime.now() - timedelta(days=2)).date())
    old_day = storage.create_day_folder((datetime.now() - timedelta(days=20)).date())
    storage._write_json(recent_day / "day_metadata.json", {"date": recent_day.name, "status": "ended"})
    storage._write_json(old_day / "day_metadata.json", {"date": old_day.name, "status": "ended"})

    window = MainWindow(storage, recorder)
    window.open_archive()
    window.archive_week_button.click()
    archive_text = "\n".join(label.text() for label in window.pages.widget(2).findChildren(QLabel))

    assert recent_day.name in archive_text
    assert old_day.name not in archive_text

    window.close()
    app.processEvents()
```

- [ ] **Step 2: Run failing tests**

Run:

```powershell
python -m pytest tests/test_ui.py::test_archive_search_filters_days_and_shows_compact_matches tests/test_ui.py::test_archive_week_filter_hides_older_days -q
```

Expected: fail because filter buttons and result list are missing.

- [ ] **Step 3: Add period buttons**

In `_create_archive_page`, below search input, add:

```python
        filter_layout = QHBoxLayout()
        filter_layout.setSpacing(8)
        self.archive_week_button = self._add_button(filter_layout, "Неделя", self.set_archive_week_filter)
        self.archive_month_button = self._add_button(filter_layout, "Месяц", self.set_archive_month_filter)
        self.archive_all_button = self._add_button(filter_layout, "Все", self.clear_archive_date_filter)
        filter_layout.addStretch(1)
        layout.addLayout(filter_layout)
```

Add methods:

```python
    def set_archive_week_filter(self) -> None:
        self.archive_date_filter = ArchiveDateFilter.week(datetime.now())
        self.refresh_archive()

    def set_archive_month_filter(self) -> None:
        self.archive_date_filter = ArchiveDateFilter.month(datetime.now())
        self.refresh_archive()

    def clear_archive_date_filter(self) -> None:
        self.archive_date_filter = None
        self.refresh_archive()
```

- [ ] **Step 4: Add compact match list**

In `_create_archive_page`, between filters and splitter:

```python
        self.archive_matches_layout = QVBoxLayout()
        self.archive_matches_layout.setSpacing(4)
        layout.addLayout(self.archive_matches_layout)
```

In `refresh_archive`, call:

```python
        self._refresh_archive_matches()
```

Add:

```python
    def _refresh_archive_matches(self) -> None:
        self._clear_layout(self.archive_matches_layout)
        if not self.archive_query:
            return
        summary = QLabel(f"Найдено: {len(self.archive_matches)} совпадений")
        summary.setObjectName("sectionHint")
        self.archive_matches_layout.addWidget(summary)
        for match in self.archive_matches[:6]:
            button = QPushButton(f"{match.kind} · {match.title} — {match.snippet}")
            button.setObjectName("secondaryButton")
            button.clicked.connect(lambda checked=False, current=match: self.select_archive_match(current))
            self.archive_matches_layout.addWidget(button)
```

Add:

```python
    def select_archive_match(self, match: ArchiveSearchMatch) -> None:
        self.selected_archive_day_folder = match.day_folder
        self.selected_archive_meeting_folder = match.meeting_folder
        self.archive_selected_material = "day_summary" if match.meeting_folder is None else (
            "transcript" if match.kind == "Транскрипт" else "meeting_summary"
        )
        self.refresh_archive()
```

- [ ] **Step 5: Run search/filter tests**

Run:

```powershell
python -m pytest tests/test_ui.py::test_archive_search_filters_days_and_shows_compact_matches tests/test_ui.py::test_archive_week_filter_hides_older_days -q
```

Expected: `2 passed`.

- [ ] **Step 6: Commit**

```powershell
git add app/ui/main_window.py tests/test_ui.py
git commit -m "Добавить поиск и фильтры дат Архива"
```

---

### Task 5: Детали дня, встреча и read-only transcript

**Files:**
- Modify: `app/ui/main_window.py`
- Test: `tests/test_ui.py`

- [ ] **Step 1: Write failing tests for detail panel**

Add:

```python
def test_archive_meeting_detail_shows_summary_and_readonly_transcript(tmp_path: Path) -> None:
    app = QApplication.instance() or QApplication([])
    recorder = NoopRecorder()
    storage = StorageService(tmp_path, recorder)
    day_folder = storage.create_day_folder((datetime.now() - timedelta(days=1)).date())
    storage._write_json(day_folder / "day_metadata.json", {"date": day_folder.name, "status": "ended"})
    meeting = storage.create_meeting_folder("Архивная встреча", datetime.fromisoformat(f"{day_folder.name}T09:30:00"))
    storage.save_meeting_summary_draft(meeting, "# Итоги встречи\n")
    (meeting / "transcript.md").write_text("Текст расшифровки", encoding="utf-8")

    window = MainWindow(storage, recorder)
    window.open_archive()
    window.select_archive_meeting(meeting)
    window.show_archive_transcript(meeting)

    assert window.archive_editor.isReadOnly()
    assert "Текст расшифровки" in window.archive_editor.toPlainText()

    window.close()
    app.processEvents()
```

- [ ] **Step 2: Run failing test**

Run:

```powershell
python -m pytest tests/test_ui.py::test_archive_meeting_detail_shows_summary_and_readonly_transcript -q
```

Expected: fail because methods and editor do not exist.

- [ ] **Step 3: Add archive editor and meeting cards**

In `_create_archive_page`, create the detail editor once:

```python
        self.archive_editor = QPlainTextEdit()
        self.archive_editor.setObjectName("reviewEditor")
```

In `_refresh_archive_detail`, render meetings with buttons:

```python
        for meeting in self._visible_archive_meetings(day):
            self.archive_detail_layout.addWidget(self._create_archive_meeting_card(meeting))
```

Add:

```python
    def _visible_archive_meetings(self, day: ArchiveDay) -> list:
        if not self.archive_query:
            return day.meetings
        matching_folders = {
            match.meeting_folder
            for match in self.archive_matches
            if match.day_folder == day.folder and match.meeting_folder is not None
        }
        return [meeting for meeting in day.meetings if meeting.folder in matching_folders]

    def _create_archive_meeting_card(self, meeting) -> QWidget:
        layout = QVBoxLayout()
        title = QLabel(meeting.title)
        title.setObjectName("meetingHeaderLabel")
        layout.addWidget(title)
        actions = QHBoxLayout()
        self._add_button(actions, "Редактировать итоги", lambda checked=False, folder=meeting.folder: self.edit_archive_meeting_summary(folder))
        self._add_button(actions, "Просмотреть transcript", lambda checked=False, folder=meeting.folder: self.show_archive_transcript(folder))
        actions.addStretch(1)
        layout.addLayout(actions)
        return self._create_card(meeting.title, layout)

    def select_archive_meeting(self, meeting_folder: Path) -> None:
        self.selected_archive_meeting_folder = meeting_folder
        self.archive_selected_material = "meeting_summary"
        self.edit_archive_meeting_summary(meeting_folder)

    def edit_archive_meeting_summary(self, meeting_folder: Path) -> None:
        self.selected_archive_meeting_folder = meeting_folder
        self.archive_selected_material = "meeting_summary"
        self.archive_editor.setReadOnly(False)
        self.archive_editor.setPlainText(self.storage.read_meeting_summary_draft(meeting_folder))
        self._show_archive_editor("Итоги встречи")

    def show_archive_transcript(self, meeting_folder: Path) -> None:
        self.selected_archive_meeting_folder = meeting_folder
        self.archive_selected_material = "transcript"
        transcript_path = Path(meeting_folder) / "transcript.md"
        text = transcript_path.read_text(encoding="utf-8") if transcript_path.is_file() else "Transcript пока не найден."
        self.archive_editor.setReadOnly(True)
        self.archive_editor.setPlainText(text)
        self._show_archive_editor("Транскрипт")

    def _show_archive_editor(self, title: str) -> None:
        self._clear_layout(self.archive_detail_layout)
        self.archive_detail_layout.addWidget(QLabel(title))
        self.archive_detail_layout.addWidget(self.archive_editor, 1)
```

- [ ] **Step 4: Run detail test**

Run:

```powershell
python -m pytest tests/test_ui.py::test_archive_meeting_detail_shows_summary_and_readonly_transcript -q
```

Expected: `1 passed`.

- [ ] **Step 5: Commit**

```powershell
git add app/ui/main_window.py tests/test_ui.py
git commit -m "Добавить просмотр материалов в Архиве"
```

---

### Task 6: Редактирование итогов и сохранение final

**Files:**
- Modify: `app/ui/main_window.py`
- Test: `tests/test_ui.py`

- [ ] **Step 1: Write failing tests for saving drafts and finals**

Add:

```python
def test_archive_saves_meeting_and_day_summary_drafts_and_finals(tmp_path: Path) -> None:
    app = QApplication.instance() or QApplication([])
    recorder = NoopRecorder()
    storage = StorageService(tmp_path, recorder)
    day_folder = storage.create_day_folder((datetime.now() - timedelta(days=1)).date())
    storage._write_json(day_folder / "day_metadata.json", {"date": day_folder.name, "status": "ended"})
    meeting = storage.create_meeting_folder("Архивная встреча", datetime.fromisoformat(f"{day_folder.name}T09:30:00"))

    window = MainWindow(storage, recorder)
    window.open_archive()
    window.edit_archive_meeting_summary(meeting)
    window.archive_editor.setPlainText("# Новый черновик встречи\n")
    window.save_archive_draft()
    window.save_archive_final()

    assert (meeting / "summary_draft.md").read_text(encoding="utf-8") == "# Новый черновик встречи\n"
    assert (meeting / "summary_final.md").read_text(encoding="utf-8") == "# Новый черновик встречи\n"

    window.edit_archive_day_summary(day_folder)
    window.archive_editor.setPlainText("# Новый черновик дня\n")
    window.save_archive_draft()
    window.save_archive_final()

    assert (day_folder / "00_day_summary_draft.md").read_text(encoding="utf-8") == "# Новый черновик дня\n"
    assert (day_folder / "00_day_summary_final.md").read_text(encoding="utf-8") == "# Новый черновик дня\n"

    window.close()
    app.processEvents()
```

- [ ] **Step 2: Run failing test**

Run:

```powershell
python -m pytest tests/test_ui.py::test_archive_saves_meeting_and_day_summary_drafts_and_finals -q
```

Expected: fail because save actions are missing.

- [ ] **Step 3: Add editor actions**

Add archive action buttons in `_show_archive_editor`:

```python
        actions = QHBoxLayout()
        self._add_button(actions, "Сохранить черновик", self.save_archive_draft)
        self._add_button(actions, "Сохранить финал", self.save_archive_final)
        self._add_button(actions, "Отмена", self.refresh_archive)
        actions.addStretch(1)
        self.archive_detail_layout.addLayout(actions)
```

Add:

```python
    def edit_archive_day_summary(self, day_folder: Path) -> None:
        self.selected_archive_day_folder = day_folder
        self.selected_archive_meeting_folder = None
        self.archive_selected_material = "day_summary"
        self.archive_editor.setReadOnly(False)
        self.archive_editor.setPlainText(self.storage.read_day_summary_draft(day_folder))
        self._show_archive_editor("Итоги дня")

    def save_archive_draft(self) -> None:
        if self.archive_editor.isReadOnly():
            return
        text = self.archive_editor.toPlainText()
        if self.archive_selected_material == "day_summary" and self.selected_archive_day_folder is not None:
            self.storage.save_day_summary_draft(self.selected_archive_day_folder, text)
            self.status_label.setText("Черновик итогов дня сохранен локально.")
        elif self.archive_selected_material == "meeting_summary" and self.selected_archive_meeting_folder is not None:
            self.storage.save_meeting_summary_draft(self.selected_archive_meeting_folder, text)
            self.status_label.setText("Черновик итогов встречи сохранен локально.")
        self.refresh_archive()

    def save_archive_final(self) -> None:
        if self.archive_editor.isReadOnly():
            return
        text = self.archive_editor.toPlainText()
        if self.archive_selected_material == "day_summary" and self.selected_archive_day_folder is not None:
            self.storage.save_day_summary_final(self.selected_archive_day_folder, text)
            self.status_label.setText("Финальные итоги дня сохранены локально. Черновик не удален.")
        elif self.archive_selected_material == "meeting_summary" and self.selected_archive_meeting_folder is not None:
            self.storage.save_meeting_summary_final(self.selected_archive_meeting_folder, text)
            self.status_label.setText("Финальные итоги встречи сохранены локально. Черновик не удален.")
        self.refresh_archive()
```

- [ ] **Step 4: Wire day summary card**

In `_archive_day_summary_layout`, add buttons:

```python
        self._add_button(actions, "Редактировать", lambda checked=False, folder=day.folder: self.edit_archive_day_summary(folder))
        self._add_button(actions, "Сформировать итоги дня", lambda checked=False, folder=day.folder: self.request_archive_day_summary_update(folder), "primaryButton")
```

Create `request_archive_day_summary_update` as a wrapper around existing `_request_day_summary_update` in Task 7.

- [ ] **Step 5: Run save test**

Run:

```powershell
python -m pytest tests/test_ui.py::test_archive_saves_meeting_and_day_summary_drafts_and_finals -q
```

Expected: `1 passed`.

- [ ] **Step 6: Commit**

```powershell
git add app/ui/main_window.py tests/test_ui.py
git commit -m "Добавить редактирование итогов в Архиве"
```

---

### Task 7: Завершение прошлых дней, повторная обработка и обновление итогов

**Files:**
- Modify: `app/ui/main_window.py`
- Test: `tests/test_ui.py`

- [ ] **Step 1: Write failing tests for processing actions**

Add:

```python
def test_archive_finish_active_past_day_recovers_meetings_and_requests_day_summary(tmp_path: Path) -> None:
    app = QApplication.instance() or QApplication([])
    recorder = NoopRecorder()
    storage = StorageService(tmp_path, recorder)
    past_day = storage.create_day_folder((datetime.now() - timedelta(days=1)).date())
    storage._write_json(past_day / "day_metadata.json", {"date": past_day.name, "status": "active"})
    meeting = storage.create_meeting_folder("Прошлая встреча", datetime.fromisoformat(f"{past_day.name}T09:30:00"))
    metadata = storage.read_meeting_metadata(meeting)
    metadata.update({"status": "ended", "processing_status": "running"})
    storage.write_metadata(meeting, metadata)
    requested: list[Path] = []

    window = MainWindow(storage, recorder)
    window._request_day_summary_update = lambda day_folder, force=False: requested.append(day_folder) or "ok"
    window.finish_archive_workday(past_day)

    day_metadata = storage.read_day_metadata(past_day)
    meeting_metadata = storage.read_meeting_metadata(meeting)
    assert day_metadata["status"] == "ended"
    assert meeting_metadata["processing_status"] == "pending"
    assert requested == [past_day]

    window.close()
    app.processEvents()


def test_archive_reprocess_meeting_uses_existing_queue(tmp_path: Path) -> None:
    app = QApplication.instance() or QApplication([])
    recorder = NoopRecorder()
    storage = StorageService(tmp_path, recorder)
    past_day = storage.create_day_folder((datetime.now() - timedelta(days=1)).date())
    storage._write_json(past_day / "day_metadata.json", {"date": past_day.name, "status": "ended"})
    meeting = storage.create_meeting_folder("Прошлая встреча", datetime.fromisoformat(f"{past_day.name}T09:30:00"))
    metadata = storage.read_meeting_metadata(meeting)
    metadata.update({
        "status": "ended",
        "processing_status": "completed",
        "summary_status": "draft_created",
        "recording_path": str(meeting / "recording.mkv"),
    })
    (meeting / "recording.mkv").write_text("recording", encoding="utf-8")
    storage.write_metadata(meeting, metadata)

    window = MainWindow(storage, recorder)
    window.archive_reprocess_meeting(meeting)

    assert storage.read_meeting_metadata(meeting)["processing_status"] == "pending"

    window.close()
    app.processEvents()
```

- [ ] **Step 2: Run failing tests**

Run:

```powershell
python -m pytest tests/test_ui.py::test_archive_finish_active_past_day_recovers_meetings_and_requests_day_summary tests/test_ui.py::test_archive_reprocess_meeting_uses_existing_queue -q
```

Expected: fail because archive action methods do not exist.

- [ ] **Step 3: Implement archive day finish action**

Add:

```python
    def finish_archive_workday(self, day_folder: Path) -> None:
        try:
            self.storage.end_workday_folder(day_folder)
        except ValueError as error:
            self.status_label.setText(str(error))
            self.refresh_archive()
            return
        recovered = self.storage.recover_interrupted_meeting_processing(day_folder)
        pending = self.storage.list_pending_meeting_processing_folders(day_folder)
        for meeting_folder in pending:
            self._enqueue_meeting_processing(meeting_folder)
        if pending or recovered:
            self.storage.mark_day_summary_waiting(day_folder)
        self._request_day_summary_update(day_folder, force=False)
        self._refresh_past_workday_recovery_card()
        self.refresh_archive()
```

In `_archive_day_summary_layout`, show `Завершить день` when `day.metadata.get("status") == "active"`:

```python
        if day.metadata.get("status") == "active":
            self._add_button(actions, "Завершить день", lambda checked=False, folder=day.folder: self.finish_archive_workday(folder), "primaryButton")
```

- [ ] **Step 4: Implement archive reprocess and day summary wrapper**

Add:

```python
    def archive_reprocess_meeting(self, meeting_folder: Path) -> None:
        self.reprocess_meeting(meeting_folder)
        self.refresh_archive()

    def request_archive_day_summary_update(self, day_folder: Path) -> None:
        self._request_day_summary_update(day_folder, force=True)
        self.refresh_archive()
```

In `_create_archive_meeting_card`, add:

```python
        self._add_button(actions, "Повторить обработку", lambda checked=False, folder=meeting.folder: self.archive_reprocess_meeting(folder))
```

Disable or hide the button if `_can_reprocess_meeting(meeting.folder, meeting.metadata)` is false.

- [ ] **Step 5: Run processing action tests**

Run:

```powershell
python -m pytest tests/test_ui.py::test_archive_finish_active_past_day_recovers_meetings_and_requests_day_summary tests/test_ui.py::test_archive_reprocess_meeting_uses_existing_queue -q
```

Expected: `2 passed`.

- [ ] **Step 6: Commit**

```powershell
git add app/ui/main_window.py tests/test_ui.py
git commit -m "Подключить обработку прошлых дней в Архиве"
```

---

### Task 8: UI polish, PROJECT_STATE и полная проверка

**Files:**
- Modify: `app/ui/main_window.py`
- Modify: `tests/test_ui.py`
- Modify: `PROJECT_STATE.md`

- [ ] **Step 1: Replace stale placeholder test text**

Ensure no UI test still expects:

```text
Архив пока не реализован
```

Run:

```powershell
rg -n "Архив пока не реализован|read-only карточки встреч|Что будет позже" tests app PROJECT_STATE.md
```

Expected: no matches in `app` or active tests. `PROJECT_STATE.md` may keep historical changelog references.

- [ ] **Step 2: Add final UI assertions**

Add one integration-style test:

```python
def test_archive_page_contains_expected_controls(tmp_path: Path) -> None:
    app = QApplication.instance() or QApplication([])
    recorder = NoopRecorder()
    storage = StorageService(tmp_path, recorder)
    past_day = storage.create_day_folder((datetime.now() - timedelta(days=1)).date())
    storage._write_json(past_day / "day_metadata.json", {"date": past_day.name, "status": "ended"})

    window = MainWindow(storage, recorder)
    window.open_archive()
    archive_text = "\n".join(label.text() for label in window.pages.widget(2).findChildren(QLabel))
    buttons = "\n".join(button.text() for button in window.pages.widget(2).findChildren(QPushButton))

    assert "Архив" in archive_text
    assert "Прошлые дни" in archive_text
    assert "Итоги дня" in archive_text
    assert "Неделя" in buttons
    assert "Месяц" in buttons
    assert "Все" in buttons

    window.close()
    app.processEvents()
```

- [ ] **Step 3: Update PROJECT_STATE**

Add a changelog entry in `PROJECT_STATE.md`:

```markdown
- Ветка `codex/archive`: реализован первый полноценный раздел `Архив` для прошлых рабочих дней. Архив исключает сегодняшний день, показывает прошлые дни свежими сверху, поддерживает поиск по датам, названиям, итогам и transcript, быстрые периоды `Неделя` / `Месяц` / `Все`, редактирование итогов дня и встречи, read-only transcript, открытие папок, завершение незавершенных прошлых дней, повторную обработку встреч и обновление итогов дня через общую очередь. Этап 8 остается `На проверке`, этап 9 не начинался. Проверки: `python -m pytest` — указать фактическое количество passed; `python -m compileall -q app` — указать фактический результат.
```

After running checks, replace the wording after each dash with the exact observed result. Do not set any stage to `Готово`.

- [ ] **Step 4: Run focused tests**

Run:

```powershell
python -m pytest tests/test_archive.py tests/test_storage.py tests/test_ui.py -q
```

Expected: all selected tests pass.

- [ ] **Step 5: Run full verification**

Run:

```powershell
python -m pytest
python -m compileall -q app
```

Expected: full test suite passes; compileall exits successfully.

- [ ] **Step 6: Manual smoke check**

Run the app locally:

```powershell
python -m app.main
```

Manual checks:

- open `Архив`;
- confirm today is absent;
- select a past day;
- search by meeting title;
- search by transcript text;
- edit a meeting summary draft;
- save final meeting summary;
- open transcript and confirm editor is read-only;
- clear search and confirm all meetings return;
- verify no audio/video/transcript/summary personal data is staged.

- [ ] **Step 7: Commit final polish and state**

```powershell
git add app/ui/main_window.py tests/test_ui.py PROJECT_STATE.md
git commit -m "Завершить первый раздел Архива"
```

---

## Final verification before PR

Run:

```powershell
git status --short
python -m pytest
python -m compileall -q app
git diff --stat origin/main...HEAD
```

Expected:

- no untracked secrets or data files;
- tests pass;
- compileall succeeds;
- diff contains only code, tests, docs/state needed for Архив.

Then create a PR with Russian title and body. Suggested title:

```text
Добавить раздел Архив прошлых рабочих дней
```

PR body should mention:

- прошлые дни без сегодняшнего;
- поиск по датам, названиям, итогам и transcript;
- редактирование итогов, read-only transcript;
- завершение прошлых дней и повторная обработка через общую очередь;
- local-first behavior and external AI constraints;
- exact verification results.

## Self-review checklist

- Spec coverage: все требования из `docs/superpowers/specs/2026-06-14-archive-design.md` покрыты задачами 1-8.
- Placeholder scan: запрещенных маркеров незавершенного плана не найдено.
- Type consistency: `ArchiveDay`, `ArchiveMeeting`, `ArchiveSearchMatch`, `ArchiveDateFilter` определены в Task 2 и используются в UI-задачах с теми же именами.
- Scope control: календарная визуализация, редактирование transcript, отдельные режимы draft/final и изменение структуры папок не включены.
