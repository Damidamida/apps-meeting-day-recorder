# Meeting Day Recorder — состояние проекта

## 1. Цель продукта

Локальное Windows-приложение для ручной фиксации ad hoc-созвонов в течение рабочего дня, последующей транскрипции, подготовки итогов по каждой встрече и общих итогов дня, ревью пользователем и сохранения финальных файлов локально.

## 2. Основной сценарий

Пользователь начинает рабочий день, вручную запускает и завершает встречи, которые часто появляются на лету из чатов, завершает рабочий день, проверяет черновики итогов, редактирует их и сохраняет финальные файлы.

## 3. Важные ограничения

- В MVP нет интеграции с почтой, календарем и мессенджерами.
- Встречи часто формируются ad hoc из чатов.
- Основной сценарий MVP — ручные кнопки `Start workday`, `Start meeting`, `End meeting`, `End workday`.
- Хранение данных остается local-first.
- Нельзя коммитить записи, аудио, транскрипты с персональными данными, `config.yaml`, `.env` и секреты.
- OBS, ffmpeg, OpenAI API и AI-суммаризация добавляются только на соответствующих этапах.
- Нельзя пушить напрямую в `main`.
- Работа идет через отдельные ветки `codex/*` и PR.
- Весь пользовательский текст должен быть на русском языке.

## 4. Правила языка

- Заголовки и описания PR — на русском.
- `README.md` и `PROJECT_STATE.md` — на русском.
- Пользовательский UI — на русском.
- Ошибки и статусы приложения — на русском.
- Метки экрана ревью, шаблоны итогов, метки задач и заголовки генерируемых Markdown-файлов — на русском.
- Английский допустим только для кода, технических команд, библиотек, внешних API и обязательных технических литералов.

## 5. Правила работы Codex с контекстом

- Перед задачей Codex обязан прочитать `AGENTS.md` и `PROJECT_STATE.md`.
- Перед задачей Codex обязан сверить репозиторий, базовую ветку, текущий этап и ограничения.
- После завершенной задачи Codex обязан обновить `PROJECT_STATE.md` в той же ветке и том же PR.
- Изменения архитектуры, статуса, плана, рисков, файлов и следующего шага отражаются в `PROJECT_STATE.md`.
- Если `PROJECT_STATE.md` отсутствует или противоречит задаче, Codex обязан остановиться и запросить уточнение.
- Важные решения нельзя удалять, пока они явно не заменены новым решением.

## 6. Ветвление и состояние репозитория

- Каноническая базовая и default branch — `main`.
- Рабочие ветки задач используют префикс `codex/*`.
- Ранее возникла путаница с default branch.
- PR #4 синхронизировал `main` с `codex/init-mvp-skeleton`.
- Будущие задачи должны ответвляться от актуальной `main`.

## 7. План MVP по итерациям

Статусы этапов:

- `Сделать` — этап еще не начат.
- `На проверке` — этап реализован в ветке и PR, автоматические проверки прошли, но человек еще не принял результат.
- `Готово` — этап проверен и принят человеком.

Codex не имеет права самостоятельно переводить этап в статус `Готово`. После реализации этап получает статус `На проверке`. Следующий этап не становится текущим до принятия предыдущего этапа человеком.

| Этап | Цель | Ожидаемый результат | Статус |
| --- | --- | --- | --- |
| 1. Skeleton / foundation | Создать безопасный каркас приложения | PySide6-окно, конфигурация, сервисы-заглушки, базовые тесты | Готово |
| 2. Local workday/meeting flow без OBS | Реализовать локальный жизненный цикл | Метаданные дня и встреч, черновики, восстановление после перезапуска | Готово |
| 3. Review UI | Добавить локальное ревью черновиков | Просмотр, редактирование и сохранение итогов и задач | Готово |
| 4. OBS integration | Подключить запись | Управление локальной записью через OBS | Готово |
| 5. FFmpeg audio extraction | Извлекать аудио | Локальное получение аудиофайлов из записи | Готово |
| 5.1. Windows launcher | Упростить локальный запуск | Запуск приложения двойным кликом через `.cmd` | Готово |
| 6. Transcription service | Добавить транскрипцию | Локальный сервис подготовки транскрипта | Готово |
| 7. Summary generation | Добавить генерацию итогов | Черновик итогов одной встречи из готового transcript | Готово |
| 8. UX/safety polish | Усилить удобство и защиту данных | Проверки, понятные ошибки, безопасные сценарии | На проверке |
| 8.1. AI Tunnel audio chunking | Сделать внешнюю транскрипцию устойчивее на длинных встречах | Chunking `audio.wav`, прогресс частей, retry и диагностика ошибок AI Tunnel | Готово |
| 9. Manual smoke test | Проверить рабочий маршрут | Подтвержденный ручной сценарий | Сделать |
| 10. Windows packaging | Собрать приложение | Устанавливаемая Windows-сборка | Сделать |

## 8. Выполненная работа

- PR #1: создан skeleton / foundation.
- PR #2: добавлен локальный жизненный цикл рабочего дня и встреч с восстановлением после перезапуска.
- PR #4: `main` синхронизирован с прежней default branch `codex/init-mvp-skeleton`.

Уже реализовано:

- PySide6-каркас приложения;
- `.gitignore`;
- `README.md`;
- `config.yaml.example`;
- `AGENTS.md`;
- `StorageService`;
- безопасные имена папок;
- `day_metadata.json`;
- `meeting_metadata.json`;
- `transcript.md`;
- `transcript.json`;
- `summary_draft.md`;
- `00_day_summary_draft.md`;
- `00_tasks_draft.md`;
- `summary_final.md`;
- `00_day_summary_final.md`;
- `00_tasks_final.md`;
- восстановление активного рабочего дня и активной встречи после перезапуска;
- история событий рабочего дня `started`, `ended`, `reopened`;
- переоткрытие завершенного рабочего дня в той же папке без потери встреч, черновиков и финальных файлов;
- перенос прежнего `ended_at` legacy-файлов в историю событий при переоткрытии дня без дублирования события `ended`;
- русская навигационная оболочка со страницами рабочего дня, ревью и справки;
- локальное редактирование и сохранение черновиков итогов встречи, итогов дня и задач;
- сохранение финальных Markdown-файлов без удаления черновиков;
- безопасная OBS-интеграция с обязательной пользовательской записью через OBS WebSocket;
- запуск и остановка записи встречи через OBS WebSocket при настроенном подключении;
- сохранение статуса записи и пути, если OBS возвращает его после остановки;
- индикатор состояния OBS и ручная проверка подключения в UI;
- локальное извлечение `audio.wav` из OBS-записи через FFmpeg;
- сохранение `audio_status`, `audio_path`, `audio_extracted_at` или `audio_error` в metadata встречи;
- безопасное завершение встречи при отсутствии записи, недоступном FFmpeg или ошибке извлечения аудио;
- Windows launcher `start_meeting_day_recorder.cmd` для запуска приложения двойным кликом через локальную `.venv`;
- launcher добавляет локальный каталог `.venv\Scripts` в `PATH`, чтобы optional Whisper CLI был доступен приложению при запуске двойным кликом;
- слой транскрипции с общей архитектурой `Transcriber`;
- первая локальная реализация транскрипции через optional Whisper CLI с моделью `base` по умолчанию;
- optional backend `faster-whisper` для ускоренной локальной транскрипции без отправки аудио во внешние сервисы;
- формирование `transcript.md` и `transcript.json` из `audio.wav`;
- сохранение `transcription_status`, `transcription_provider`, `transcript_path`, `transcript_json_path`, `transcribed_at` или `transcription_error` в metadata встречи;
- безопасное завершение встречи, если транскрипция пропущена, Whisper недоступен или CLI завершился с ошибкой;
- слой summary generation с общей архитектурой `Summarizer`;
- OpenAI backend для подготовки `summary_draft.md` одной встречи из готового текстового транскрипта;
- безопасная загрузка API key из переменной окружения или внешнего `.env.local` без записи ключа в git, logs, metadata или README;
- настройка `summary.base_url` для работы через OpenAI-совместимые endpoints, включая ProxyAPI `https://api.proxyapi.ru/openai/v1`;
- отправка в OpenAI только текста `transcript.md` / `transcript.json`, без аудио и видео;
- безопасные статусы summary `disabled`, `skipped`, `openai_unavailable`, `failed`, `draft_created`;
- разбиение длинного transcript на chunks для обработки одной длинной встречи;
- суммирование `summary_usage` по всем chunk-запросам и финальному summary-запросу для длинных transcript;
- кнопка `Проверить готовность` со статусами OBS, FFmpeg, Whisper, summary, API key, endpoint и папки данных;
- визуальная статусная модель pipeline встречи;
- явное определение пустого или неготового transcript без отправки пустого текста во внешний сервис;
- фоновый pipeline обработки встречи для FFmpeg, Whisper и summary, чтобы UI не зависал;
- неблокирующее завершение встречи: после остановки записи можно сразу начать следующий созвон, а обработка предыдущей встречи продолжается в очереди;
- безопасная config validation с fallback на defaults при ошибках YAML и некорректных типах;
- README-инструкция ручной проверки полного сценария;
- тесты.

## 9. Текущий статус

Этапы 1–7 завершены и приняты пользователем. Промежуточный этап 5.1 — Windows launcher также завершен и принят. Технический fix кодировки русских пользовательских строк из PR #14 принят.

Этап 8 `UX/safety polish` реализован в ветке/PR и находится на проверке. PR #23 с неблокирующей обработкой встреч был смержен и вручную проверен: после остановки записи следующую встречу можно начать сразу, а FFmpeg/Whisper/Summary по предыдущей встрече выполняются в фоновой очереди.

На ручной проверке также выявлено, что локальный Whisper CLI может работать примерно столько же времени, сколько длилась запись. PR #24 с optional backend `faster-whisper` был смержен и проверен вручную на коротких встречах: локальная транскрипция через `faster-whisper` работает, аудио остается локальным и не отправляется во внешние сервисы.

Ручная проверка полного сценария прошла успешно: приложение запущено через Windows launcher, рабочий день начат, встреча начата и записана через OBS, после завершения встречи FFmpeg создал `audio.wav`, локальный Whisper создал `transcript.md` и `transcript.json`, OpenAI-compatible endpoint / ProxyAPI создал `summary_draft.md`, metadata встречи обновилась корректно.

Подтверждено, что для summary generation во внешний OpenAI-compatible endpoint / ProxyAPI отправляется только текст transcript. Аудио и видео во внешний сервис не отправляются.

Для этапа 8 добавлены readiness check, визуальная статусная модель pipeline, явное отображение пустого transcript, background worker для тяжелых шагов обработки встречи, неблокирующее начало следующего созвона после остановки записи, optional faster-whisper backend для локального ускорения транскрипции, безопасная config validation, защита секретов и README-инструкция ручного полного сценария.

PR #25 со светлым UI-каркасом главного окна был смержен после визуальной проверки пользователем. PR #26 с новой композицией экрана `Рабочий день` был смержен после визуальной проверки пользователем. PR #27 с новым отображением `Pipeline встречи` был смержен после визуальной проверки пользователем. PR #28 с визуальной доводкой readiness-карточек был смержен после визуальной проверки пользователем.

PR #33 синхронизировал `main` с финальным UI-стеком PR #30–#32. `main` снова содержит актуальное UI-состояние вкладок `Рабочий день`, `Ревью`, `Настройки`, `Архив` и `Справка`.

PR #34, ветка `codex/day-summary-pipeline`, был смержен после ручной проверки пользователем: `Итоги дня` стали отдельной карточкой в блоке `Встречи за день` на вкладке `Рабочий день` и первой карточкой в списке на вкладке `Ревью`. Карточка `Итоги дня` получила собственный pipeline: сбор итогов встреч, проверка наличия summary, генерация выжимки через OpenAI-compatible endpoint / ProxyAPI и формирование ссылок на transcript встреч внутри приложения.

Автоматическое формирование `Итогов дня` запускается после нажатия `Завершить рабочий день`. Если обработка встреч еще идет в фоне, `Итоги дня` переходят в состояние ожидания и стартуют после завершения очереди. Кнопка `Обновить итоги дня` доступна только в карточке `Итоги дня` на вкладке `Рабочий день`. При повторном завершении переоткрытого дня итог не пересобирается без новых встреч; если появились новые встречи, существующий черновик дополняется через OpenAI-compatible endpoint / ProxyAPI.

Для дневных итогов во внешний сервис отправляются только тексты `summary_final.md` или `summary_draft.md` встреч. Аудио, видео и transcript во внешний сервис для дневных итогов не отправляются. Если у встречи отсутствует summary, она все равно включается в дневной итог с явной пометкой об отсутствии summary.

Договоренность по задачам дня сохраняется: функционал задач дня скрыт из UI, но существующая файловая логика `00_tasks_draft.md` / `00_tasks_final.md` не удаляется, чтобы не ломать старые данные.

Ветка `codex/dark-theme-ui` была смержена после проверки пользователем. В ней реализованы полноценная темная тема основного UI, отдельный выбор темы для floating control и защита от некачественной транскрипции. Тема основного окна применяется сразу после `Сохранить настройки`; floating control может наследовать тему приложения или использовать отдельную светлую/темную тему. Темная тема покрывает страницы внутри scroll-area, промежутки между карточками и подписи форм в `Рабочий день` и `Настройки`. Для `faster-whisper` включен VAD-фильтр, добавлена проверка качества transcript, подозрительная транскрипция помечается как `Транскрипция требует проверки`, summary для такого transcript не отправляется во внешний AI endpoint, а в карточке встречи доступна повторная обработка. Логика OBS, FFmpeg, записи и локального хранения не меняется.

PR #37, ветка `codex/summary-aitunnel-config`, был смержен после проверки: генерация итогов через AI Tunnel работает, в кабинетe AI Tunnel зафиксирован запрос к `gpt-5.4-mini`. Дефолтные summary-настройки используют `AITUNNEL_KEY` и `https://api.aitunnel.ru/v1/`; ProxyAPI остается возможным альтернативным custom endpoint.

В ветке `codex/aitunnel-transcription-backend` готовится точечная доработка этапа 8: добавляется optional backend `aitunnel` для внешней транскрипции `audio.wav` через AI Tunnel STT endpoint. Локальные backend `whisper_cli` и `faster_whisper` сохраняются, backend по умолчанию остается `whisper_cli`. Настройки транскрипции разделены на отдельные backend-профили, поэтому модель и параметры `whisper_cli`, `faster_whisper` и `aitunnel` не перетирают друг друга при переключении. Экран `Настройки` показывает только поля, релевантные выбранному backend; язык транскрипции фиксирован как русский, `whisper_command`, `compute_type`, `api_key_env`, `base_url` и сервисный `env_file` скрыты из обычного UI. Для AI Tunnel в UI доступен выпадающий список моделей с ценами: `Whisper Large V3 Turbo — 0.13 ₽/мин`, `Whisper Large V3 — 0.36 ₽/мин`, `Whisper 1 — 1.15 ₽/мин`; по умолчанию используется `whisper-large-v3-turbo`. Блок `Summary` в настройках также упрощен: технические поля API key, Base URL и `.env` скрыты, используется `AITUNNEL_KEY`, `https://api.aitunnel.ru/v1/` и общий `secrets.env_file`; в UI доступны рекомендуемые модели `GPT 5.4 Mini — 144 ₽/1M вход · 864 ₽/1M выход`, `GPT 5.4 Nano — 38.4 ₽/1M вход · 240 ₽/1M выход` и пункт `Другая модель AI Tunnel` для ручного ID модели. После ручной проверки исправлена рассинхронизация readiness/runtime: реальный Summary pipeline теперь подхватывает общий `secrets.env_file`, если `summary.env_file` пустой, поэтому статус `API key найден` в готовности соответствует реальной генерации итогов. При `aitunnel` во внешний сервис отправляется только `audio.wav`; видео не отправляется. Chunking длинного аудио в этом PR не реализуется, для файла действует лимит 25 МБ. Общий блок `secrets.env_file` используется для локального `.env` с API-ключами; readiness и runtime внешней транскрипции используют его для `AITUNNEL_KEY`. После `Сохранить настройки` тема применяется сразу; если нет активной встречи или фоновой обработки, следующие встречи используют новую транскрипционную конфигурацию сразу, а если обработка еще идет, текущий pipeline завершается со старой конфигурацией.

Пункт 8.1 `AI Tunnel audio chunking` принят пользователем после ручной проверки и merge PR #39 в `main`. Подтверждено, что длинный `audio.wav` для backend `aitunnel` нарезается на части, pipeline показывает прогресс (`Выполнено: N/M (%)`), chunk-транскрипция работает, дефолтный размер chunk-а уменьшен до 300 секунд, а ошибки AI Tunnel получили retry и более понятную диагностику. Chunking применяется только при явно выбранном backend `aitunnel`; локальные backend `whisper_cli` и `faster_whisper` не меняют поведение. При `aitunnel` во внешний сервис отправляется аудио WAV-частями, видео не отправляется; Summary по-прежнему отправляет только текст transcript или тексты summary встреч.

В ветке `codex/pipeline-status-model` реализована доработка backlog-пунктов BL-009 и BL-010: карточки встреч больше не используют `Завершена` как финальный пользовательский статус, показывают `Итоги готовы`, `Итоги выключены`, `Без записи`, `Ждет обработки встречи`, `Обработка` или `Требует внимания` по фактическому состоянию результата. Pipeline больше не пишет `Ждет audio.wav`, если `audio.wav` уже извлечен; сегодняшние встречи с `processing_status = pending` восстанавливаются в очередь обработки при запуске приложения.

В ветке `codex/remove-obs-disable-option` подготовлена точечная доработка этапа 8: пользовательская опция отключения OBS удалена из экрана `Настройки`, `config.yaml.example`, README и сохраняемого `config.yaml`. Старое поле `obs.enabled` в локальном `config.yaml` игнорируется для обратной совместимости и не переводит приложение в режим без записи. OBS остается обязательной частью нормального сценария записи; внутренний `NoopRecorder` сохраняется только как тестовая подмена.

В ветке `codex/safe-reprocess-button` подготовлена точечная доработка этапа 8 по backlog-пункту BL-001: кнопка `Повторить обработку` больше не показывается для активной встречи, встречи в очереди, встречи в обработке, встречи без записи и встречи со статусом `Итоги выключены`. Кнопка остается доступной для статусов `Требует внимания` и `Итоги готовы`, если доступен файл записи для безопасного повторного запуска pipeline.

PR #44, ветка `codex/recover-interrupted-pipeline`, был смержен: подготовлена точечная доработка этапа 8 по итогам review issue #19. Если приложение было закрыто или упало во время обработки встречи, сегодняшние встречи с `processing_status = running` при следующем запуске переводятся обратно в очередь как `pending` и получают понятную отметку восстановления. Уже готовые шаги pipeline (`audio.wav`, transcript, `summary_draft.md`) при таком восстановлении не запускаются повторно, чтобы не делать лишнюю работу и не тратить внешний AI-запрос повторно. Ручная кнопка `Повторить обработку` сохраняет прежний смысл и принудительно запускает обработку заново. Поврежденные `day_metadata.json` и `meeting_metadata.json` сохраняются рядом как backup `*.corrupt-*.json`, а текущий metadata-файл заменяется минимальным marker-metadata со статусом `corrupted`, чтобы приложение не падало при старте и могло отличить auto-heal от нормальных данных. Дополнительно по review CodeRabbit: старт рабочего дня умеет пересоздавать auto-healed metadata, восстановление pipeline больше не считает placeholder transcript/summary готовыми результатами, а UI не теряет путь к backup при одновременном восстановлении очереди. Чтение и атомарная запись metadata получили короткий retry на временную Windows-блокировку файла.

В ветке `codex/recovery-review-followups` подготовлен follow-up к PR #44 по Codex Review от 2026-06-07: неожиданный exception внутри pipeline переводит встречу в `processing_status = failed`, поврежденный `00_day_summary_metadata.json` восстанавливается через backup и пересоздание metadata, а auto-healed и поврежденные встречи не попадают в AI-агрегацию итогов дня как обычные встречи без summary.

В ветке `codex/storage-folder-picker` закрыт backlog-пункт BL-005: в `Настройки` → `Хранение` добавлена кнопка `Выбрать папку` со стандартным системным выбором директории. При сохранении настроек приложение создает выбранную папку, проверяет, что путь не указывает на файл, делает короткую проверку записи и сохраняет нормализованный expanded path. Если рабочий день или встреча активны, новый путь сохраняется в `config.yaml`, но текущий `StorageService` продолжает работать со старой папкой до завершения рабочего дня, чтобы не переключить активные данные на лету; после снятия блокеров отложенный путь применяется автоматически. Этап 8 остается `На проверке`, этап 9 не начинался.

В ветке `codex/visible-safety-prompts` подготовлена точечная доработка этапа 8 по backlog-пункту BL-007: добавлены понятные предупреждения при закрытии приложения во время активного созвона или фоновой обработки, а блоки `Состояние дня` и `Активный созвон` показывают причины доступности или недоступности действий. Lifecycle рабочего дня, встреч и pipeline не менялся.

Этап 8 `UX/safety polish` остается объемным и продолжается. Этап 9 `Manual smoke test` остается в статусе `Сделать` и не начинался.

Последняя проверка:

- `python -m pytest`: `153 passed`.
- `python -m compileall -q app`: успешно.

## 10. Следующий шаг

Продолжить отдельные доработки этапа 8 `UX/safety polish`. Для отложенных идей, UX-замечаний и багов ручной проверки использовать `BACKLOG.md`. Этап 9 не начинать до принятия объемного этапа 8.

## 11. Текущая структура файлов

- `AGENTS.md` — обязательные правила работы Codex.
- `BACKLOG.md` — парковка отложенных идей, UX-замечаний, багов ручной проверки и технического долга.
- `PROJECT_STATE.md` — единый источник истины о контексте проекта.
- `README.md` — краткое описание и запуск.
- `config.yaml.example` — пример локальной конфигурации.
- `start_meeting_day_recorder.cmd` — запуск приложения двойным кликом через локальную `.venv`.
- `pyproject.toml` — зависимости и настройки тестов.
- `app/main.py` — точка входа.
- `app/config.py` — загрузка конфигурации.
- `app/ui/main_window.py` — главное окно.
- `app/services/storage.py` — локальное хранение и жизненный цикл.
- `app/services/recorder.py` — безопасная OBS-интеграция и внутренняя тестовая подмена без записи.
- `app/services/readiness.py` — безопасная проверка готовности локальной среды.
- `app/services/audio.py` — локальное извлечение `audio.wav` через FFmpeg.
- `app/services/ai_errors.py` — общая классификация ошибок AI Tunnel / OpenAI-compatible endpoint и retry-логика.
- `app/services/transcription.py` — слой транскрипции через optional Whisper CLI, optional faster-whisper или optional AI Tunnel STT backend.
- `app/services/summarization.py` — слой генерации итогов встречи через безопасный `Summarizer` и OpenAI backend.
- `tests/test_storage.py` — тесты локального хранения.
- `tests/test_launcher.py` — тест launcher для поиска локальных CLI-инструментов из `.venv\Scripts`.
- `tests/test_readiness.py` — тесты проверки готовности и защиты отображения API key.
- `tests/test_summarization.py` — тесты генерации итогов, включая суммирование usage при chunking.
- `tests/test_transcription.py` — тесты локальной и внешней транскрипции, включая AI Tunnel chunking и retry.

## 12. Структура локальных данных

Фактическая структура текущей реализации:

```text
MeetingSummaries/YYYY-MM-DD/
  day_metadata.json
  00_day_summary_draft.md
  00_tasks_draft.md
  HH-MM_title/
    meeting_metadata.json
    transcript.md
    transcript.json
    summary_draft.md
    summary_final.md
  00_day_summary_final.md
  00_tasks_final.md
```

Вопрос на пересмотр перед развитием Review UI: нужен ли промежуточный каталог `meetings/` между папкой дня и папками встреч. Сейчас код его не создает.

## 13. Архитектурные решения

- Python 3.11+.
- PySide6 для desktop UI.
- YAML для конфигурации.
- JSON для метаданных.
- Markdown для итогов.
- OBS WebSocket для локального управления записью встреч.
- FFmpeg для локального извлечения `audio.wav` из OBS-записи.
- Транскрипция выполняется локально через optional Whisper CLI или optional faster-whisper backend; optional AI Tunnel STT backend можно включить явно через `transcription.backend: aitunnel`. Для AI Tunnel длинный WAV-файл может автоматически нарезаться на части с отображением прогресса в pipeline.
- AI Tunnel / OpenAI-compatible endpoint используется для генерации `summary_draft.md` одной встречи из готового текстового transcript и для генерации `00_day_summary_draft.md` из текстов summary встреч, если summary явно включен в локальном `config.yaml`. ProxyAPI остается возможным альтернативным custom endpoint.
- Сначала ручной сценарий, автоматизация позже.

## 14. Известные риски и решения на пересмотр

- Восстановление состояния после перезапуска должно сохраняться.
- Выходные файлы должны оставаться вне git.
- Нужно принять решение о промежуточной папке `meetings/`.
- Настройка OBS-сцены и аудио будет ручной предпосылкой.
- Пароль OBS WebSocket хранится только в локальном `config.yaml`, который не добавляется в git.
- `config.yaml.example` остается безопасным шаблоном без пароля; локальный рабочий `config.yaml` создается рядом с ним и не заменяет шаблон в репозитории.
- Путь записи настраивается в OBS; приложение сохраняет `recording_path`, только если OBS возвращает его после остановки записи.
- Большие записи могут потребовать политику очистки.
- Длинное WAV-аудио для внешнего AI Tunnel STT backend нарезается на части перед отправкой. Если отдельная часть все равно превышает лимит 25 МБ или формат не поддерживает локальную нарезку, встреча должна получить понятный статус ошибки без потери локальных файлов.
- Локальный Whisper CLI на модели `base` может обрабатывать запись примерно в режиме real-time; optional faster-whisper добавлен как первый локальный путь ускорения. Более тонкая настройка моделей и будущие внешние STT-провайдеры остаются возможными будущими улучшениями.
- Качество prompt для итогов нужно будет улучшать итерационно.
- AI Tunnel / OpenAI-compatible API key должен храниться только во внешнем окружении или локальном `.env.local`, который не добавляется в git.
- Генерация итогов не должна отправлять во внешний AI endpoint аудио или видео; для итогов встречи отправляется только текстовый transcript, для итогов дня — только тексты summary встреч.
- Нельзя смешивать этапы и добавлять AI или OBS раньше плана.
- Пользовательские тексты UI и генерируемых Markdown-заглушек переведены на русский язык на этапе Review UI.

## 15. Backlog

Отложенные идеи, UX-замечания, баги ручной проверки и технический долг ведутся в `BACKLOG.md`.

- Кодовое слово `В бэклог:` означает: зафиксировать пункт в `BACKLOG.md`, но не реализовывать его без отдельного запроса.
- Кодовое слово `Из бэклога:` означает: найти пункт в `BACKLOG.md` и подготовить его к обсуждению или отдельной реализации.
- Запись в `BACKLOG.md` не меняет текущий этап и не означает, что задача взята в работу.

## 16. Changelog

- Ветка `codex/visible-safety-prompts`: точечная доработка этапа 8 по backlog-пункту BL-007. При закрытии приложения во время активного созвона показывается модальное предупреждение `Идет активный созвон`, закрытие блокируется до завершения встречи. При закрытии во время фоновой обработки показывается модальное предупреждение `Идет обработка встречи` с выбором `Остаться в приложении` / `Закрыть и восстановить позже`; восстановление опирается на существующую recovery-модель pipeline. В блоках `Состояние дня` и `Активный созвон` добавлены видимые объяснения: когда нужно начать рабочий день, почему нельзя завершить день во время активной встречи, и почему завершение дня при фоновой обработке разрешено, но `Итоги дня` начнутся после завершения обработки встреч. Этап 8 остается `На проверке`, этап 9 не начинался. Проверки: `python -m pytest` — `153 passed`; `python -m compileall -q app` — успешно.
- Ветка `codex/storage-folder-picker`: точечная доработка этапа 8 по backlog-пункту BL-005. В настройках хранения добавлена кнопка `Выбрать папку`, выбранная папка сохраняется в `config.yaml`, создается при необходимости и проверяется на доступность записи. Путь, который указывает на файл, не сохраняется и показывает понятную ошибку; `~` и другие user-path формы сохраняются как нормализованный expanded path. Если рабочий день или встреча активны, новый путь сохраняется, но текущий `StorageService` не переключается на лету; папка данных применяется автоматически после завершения рабочего дня и фоновой обработки. Этап 8 остается `На проверке`, этап 9 не начинался. Проверки: `python -m pytest` — `150 passed`; `python -m compileall -q app` — успешно.
- Ветка `codex/recovery-review-followups`: follow-up к PR #44 по Codex Review от 2026-06-07. Неожиданный exception внутри pipeline теперь переводит встречу в `processing_status = failed`, сохраняет `processing_error` и не блокирует `Итоги дня` как вечный `running`; поврежденный `00_day_summary_metadata.json` восстанавливается через backup и пересоздание минимального metadata дневных итогов; auto-healed и поврежденные встречи с некорректным metadata исключаются из AI-агрегации итогов дня. Этап 8 остается `На проверке`, этап 9 не начинался. Проверки: `python -m pytest` — `145 passed`; `python -m compileall -q app` — успешно.
- PR #44, ветка `codex/recover-interrupted-pipeline`: точечная доработка этапа 8 по итогам review issue #19. При запуске приложения прерванные встречи сегодняшнего дня с `processing_status = running` восстанавливаются в очередь обработки как `pending` с отметкой `processing_recovery_status = recovered`; уже готовые шаги pipeline не повторяются при автоматическом восстановлении, но ручное `Повторить обработку` по-прежнему запускает pipeline заново. Чтение поврежденных `day_metadata.json` и `meeting_metadata.json` делает backup `*.corrupt-*.json`, записывает marker-metadata со статусом `corrupted` и не роняет восстановление состояния. По review CodeRabbit добавлена защита от placeholder transcript/summary при проверке готовности pipeline, сохранение backup-сообщения в UI, no-op sleep в retry-тестах и проверка очистки recovery-флагов в day-level metadata. Чтение и атомарная запись metadata получили retry на кратковременную Windows-блокировку, чтобы UI и фоновый pipeline не падали при одновременной работе с metadata. Этап 8 остается `На проверке`, этап 9 не начинался. Проверки в PR #44: `python -m pytest` — `141 passed`; `python -m compileall -q app` — успешно.
- Ветка `codex/safe-reprocess-button`: точечная доработка этапа 8 по backlog-пункту BL-001. Кнопка `Повторить обработку` теперь создается в раскрытой карточке встречи только для безопасных сценариев: `Требует внимания` или `Итоги готовы` при наличии файла записи. Для активной встречи, очереди, текущей обработки, встречи без записи и намеренно выключенных итогов кнопка скрыта. Этап 8 остается `На проверке`, этап 9 не начинался. Проверки: `python -m pytest` — `127 passed`; `python -m compileall -q app` — успешно.
- Ветка `codex/remove-obs-disable-option`: точечная доработка этапа 8 по удалению пользовательской возможности отключить OBS. Чекбокс `OBS включен` удален из экрана `Настройки`; `obs.enabled` удален из `DEFAULT_CONFIG`, `config.yaml.example`, README и сохраняемого `config.yaml`; старое поле `obs.enabled` в локальном конфиге игнорируется. `create_recorder` теперь всегда создает OBS-рекордер для пользовательского сценария, а `NoopRecorder` остается только внутренней тестовой подменой. Этап 8 остается `На проверке`, этап 9 не начинался. Проверки: `python -m pytest` — `125 passed`; `python -m compileall -q app` — успешно.
- Ветка `codex/pipeline-status-model`: доработка этапа 8 по backlog-пунктам BL-009 и BL-010. Добавлена честная модель пользовательских статусов встречи: `Итоги готовы`, `Итоги выключены`, `Без записи`, `Ждет обработки встречи`, `Обработка`, `Требует внимания`; статус `Завершена` больше не используется как финальный badge карточки. Pipeline-сообщения уточнены: если `audio.wav` уже готов, транскрипция не пишет `Ждет audio.wav`; `summary_status = skipped` считается проблемой при включенной генерации итогов; `summary_status = disabled` считается ожидаемым состоянием `Итоги выключены`. При запуске приложения сегодняшние встречи с `processing_status = pending` восстанавливаются в очередь обработки. Этап 8 остается `На проверке`, этап 9 не начинался. Проверки: `python -m pytest` — `124 passed`; `python -m compileall -q app` — успешно.
- PR #40, ветка `codex/mark-stage-8-1-done-add-backlog`: документационная фиксация принятия пункта 8.1 после merge PR #39. Пункт 8.1 `AI Tunnel audio chunking` переведен в `Готово`, этап 8 оставлен в работе/на проверке, этап 9 не начинался. Создан `BACKLOG.md` и перенесены 12 backlog-пунктов, ранее зафиксированных через кодовое слово `В бэклог:`. Код приложения не менялся. Проверки: `python -m pytest` — `121 passed`; `python -m compileall -q app` — успешно.
- PR #39, ветка `codex/aitunnel-audio-chunking`: пункт 8.1 `AI Tunnel audio chunking` принят после ручной проверки пользователем и merge в `main`. Для внешней транскрипции через AI Tunnel добавлен chunking длинного `audio.wav` на локальные WAV-части, последовательная отправка частей в AI Tunnel STT, сбор единого `transcript.md` и `transcript.json`, metadata `transcription_mode`, `transcription_chunk_count`, `transcription_completed_chunks` и суммирование `transcription_usage`. Pipeline встречи показывает прогресс chunk-транскрипции (`Выполнено: N/M (%)`) и retry временных ошибок. После ручной проверки `Созвон 2` дефолтный chunk уменьшен до 300 секунд, transport/timeout-ошибки без HTTP-кода тоже retry-ятся, а неизвестные ошибки chunk-а сохраняют безопасную диагностику типа исключения, короткого сообщения, номера части и количества попыток. Локальные backend `whisper_cli` и `faster_whisper`, OBS, FFmpeg, lifecycle встреч и локальное хранение не менялись. Этап 8 остается в работе/на проверке, этап 9 не начинался. Проверки в PR #39: `python -m pytest` — `121 passed`; `python -m compileall -q app` — успешно.
- PR #1: создан skeleton / foundation.
- PR #2: добавлен локальный жизненный цикл рабочего дня и встреч с восстановлением после перезапуска.
- PR #4: `main` синхронизирован с прежней default branch.
- PR #5, ветка `codex/add-project-state-memory`: добавлены единый файл памяти проекта, обязательный workflow Codex и ссылка из README. Проверки: `python -m pytest` — `13 passed`; `python -m compileall -q app` — успешно.
- PR #6, ветка `codex/review-ui`, статус этапа: `На проверке`: добавлены русская навигационная оболочка, экран ревью, сохранение черновиков и финальных файлов без удаления draft-файлов. Проверки: `python -m pytest` — `18 passed`; `python -m compileall -q app` — успешно; фоновая UI-проверка — успешно.
- После merge PR #6 на проверке уточнена логика lifecycle: завершенный рабочий день должен переоткрываться в той же папке без потери встреч, черновиков и финальных файлов.
- PR #7, ветка `codex/reopen-ended-workday`: добавлены события `started`, `ended`, `reopened` и переоткрытие завершенного рабочего дня в тот же календарный день. При переоткрытии legacy-файла прежний `ended_at` переносится в историю событий без дублирования `ended`. Проверки: `.venv\Scripts\python.exe -m pytest` — `19 passed`; `.venv\Scripts\python.exe -m compileall -q app` — успешно; фоновая UI-проверка кнопок — успешно.
- PR #8, ветка `codex/mark-stage-3-done`: этап 3 Review UI принят после ручной проверки пользователем. Исправление lifecycle из PR #7 также принято. Этап 3 переведен в статус `Готово`; этап 4 — OBS integration остается в статусе `Сделать`.
- PR #9, ветка `codex/obs-integration`, статус этапа: `На проверке`: добавлены безопасная OBS-интеграция, Noop-режим по умолчанию, запись OBS-статусов в metadata встречи, индикатор и ручная проверка OBS в UI. Проверки: `.venv\Scripts\python.exe -m pytest` — `27 passed`; `.venv\Scripts\python.exe -m compileall -q app` — успешно.
- PR #10, ветка `codex/accept-stage-4-package-discovery`: этап 4 OBS integration принят после ручной локальной проверки. Подтверждены подключение OBS WebSocket, запуск и остановка записи, metadata с путем видео, наличие картинки и звука. Добавлено явное ограничение package discovery пакетом `app`; неиспользуемое поле `obs.recording_output_dir` удалено. Этап 5 остается в статусе `Сделать` и еще не начат. Проверки: `python -m pip install -e .` — успешно; `python -m pip install -e ".[dev]"` — успешно; `python -m pytest` — `27 passed`; `python -m compileall -q app` — успешно.
- PR #11, ветка `codex/ffmpeg-audio-extraction`: этап 5 FFmpeg audio extraction принят. Локальное извлечение `audio.wav` из OBS-записи через FFmpeg работает, audio-metadata сохраняется, ошибки обрабатываются без сбоя завершения встречи. Ручная проверка FFmpeg выполнена на реальном OBS-видео во временную папку: `audio.wav` создан, `ffprobe` подтвердил `pcm_s16le`, `16000 Hz`, mono. Проверки: `python -m pip install -e ".[dev]"` — успешно; `python -m pytest` — `31 passed`; `python -m compileall -q app` — успешно.
- PR #12, ветка `codex/windows-launcher`: промежуточный этап 5.1 Windows launcher принят. `start_meeting_day_recorder.cmd` позволяет запускать приложение двойным кликом через локальную `.venv`; проверены успешный запуск и понятная русская ошибка при отсутствии `.venv`. Проверки: `python -m pip install -e ".[dev]"` — успешно; `python -m pytest` — `31 passed`; `python -m compileall -q app` — успешно.
- PR #13, ветка `codex/transcription-service`: этап 6 Transcription service принят. Добавлен слой транскрипции с контрактом `Transcriber`, локальная реализация через optional Whisper CLI и безопасные статусы `skipped`, `missing_audio`, `whisper_unavailable`, `failed`, `completed`. `transcript.md` и `transcript.json` формируются из `audio.wav`; metadata встречи получает поля транскрипции. Модель `base` выбрана как стартовая для текущей конфигурации ПК. OpenAI API не добавлялся, аудио не отправляется во внешние сервисы. Этап 7 остается в статусе `Сделать` и еще не начат. Проверки: `python -m pip install -e ".[dev]"` — успешно; `python -m pytest` — `35 passed`; `python -m compileall -q app` — успешно.
- PR #14, ветка `codex/fix-russian-encoding`: технический fix кодировки принят. Подтверждено, что tracked-файлы, metadata и transcript-файлы пишутся в UTF-8; проблема проявлялась в консольном отображении PowerShell. Launcher усилен UTF-8 режимом консоли и Python. OBS, FFmpeg, Whisper pipeline, OpenAI API, summary generation и этап 7 не менялись. Проверки: `python -m pip install -e ".[dev]"` — успешно; `python -m pytest` — `39 passed`; `python -m compileall -q app` — успешно; launcher без `.venv` показал читаемую русскую подсказку и завершился с кодом `1`.
- PR #16, ветка `codex/openai-summary-generation`: этап 7 Summary generation реализован и переведен в статус `На проверке`. Добавлен слой summary generation, OpenAI backend для подготовки `summary_draft.md` одной встречи из готового текстового transcript, безопасная загрузка API key из переменной окружения или внешнего `.env.local`, статусы ошибок без сбоя lifecycle и chunking для длинного transcript. В OpenAI отправляется только текст transcript, не аудио и не видео. Summary по умолчанию выключен и включается через локальный `config.yaml`. Этап 8 не начинался. Ручная сервисная проверка с прямым OpenAI API не завершилась из-за отсутствия доступной оплаты/валидного ключа. Unit/lifecycle проверки выполнены с mock OpenAI без реальных API-запросов. Проверки: `python -m pip install -e ".[dev]"` — успешно; `python -m pytest` — `50 passed`; `python -m compileall -q app` — успешно.
- PR #17, ветка `codex/proxyapi-summary-generation`: добавлена поддержка ProxyAPI для этапа 7 через настройку `summary.base_url` и OpenAI-совместимый endpoint `https://api.proxyapi.ru/openai/v1`. Обновлены README, `config.yaml.example`, PROJECT_STATE и тесты передачи `base_url` в OpenAI client. В ProxyAPI/OpenAI отправляется только текст transcript, не аудио и не видео. Реальная проверка ProxyAPI требует локального ProxyAPI-ключа. Этап 7 остается `На проверке`, этап 8 не начинался. Проверки: `python -m pip install -e ".[dev]"` — успешно; `python -m pytest` — `50 passed`; `python -m compileall -q app` — успешно.
- PR #18, ветка `codex/fix-launcher-whisper-path`: launcher дополнен добавлением `.venv\Scripts` в `PATH`, чтобы приложение находило локальный optional Whisper CLI при запуске двойным кликом. Добавлен тест launcher. Логика OBS, FFmpeg, Whisper pipeline, ProxyAPI/OpenAI и summary generation не менялась. Этап 7 остается `На проверке`, этап 8 не начинался. Проверки: `cmd /c "set PATH=%CD%\.venv\Scripts;%PATH%&& where whisper"` — найден локальный `.venv\Scripts\whisper.exe`; `.venv\Scripts\python.exe -m pytest` — `51 passed`; `.venv\Scripts\python.exe -m compileall -q app` — успешно.
- PR #20, ветка `codex/fix-summary-usage-aggregation`: исправлен учет `summary_usage` при chunking длинных transcript. Теперь `input_tokens` и `output_tokens` суммируются по промежуточным chunk-запросам и финальному summary-запросу. Добавлен тест на агрегацию usage. Этап 7 остается `На проверке`, этап 8 не начинался. Проверки: `.venv\Scripts\python.exe -m pytest` — `51 passed`; `.venv\Scripts\python.exe -m compileall -q app` — успешно.
- PR #21, ветка `codex/mark-stage-7-done`: этап 7 Summary generation принят после ручной проверки полного сценария. Подтверждены запуск через Windows launcher, OBS-запись, извлечение `audio.wav` через FFmpeg, локальная транскрипция через Whisper, создание `summary_draft.md` через OpenAI-compatible endpoint / ProxyAPI и корректное обновление metadata. Во внешний сервис отправляется только текст transcript, не аудио и не видео. Код приложения не менялся, обновлен только `PROJECT_STATE.md`. Этап 8 не начинался. Проверки: `.venv\Scripts\python.exe -m pytest` — `51 passed`; `.venv\Scripts\python.exe -m compileall -q app` — успешно.
- PR #22, ветка `codex/ux-safety-polish`: этап 8 UX/safety polish реализован и переведен в статус `На проверке`. Добавлены readiness check, визуальная статусная модель pipeline, явное отображение пустого transcript, background worker для тяжелых операций FFmpeg/Whisper/Summary, безопасная config validation, защита секретов и README-инструкция ручного полного сценария. Устранен потенциальный race condition в UI progress: поздние pipeline-сигналы читают metadata по сохраненной папке встречи, а не через уже сброшенный active meeting. Этап 9 не начинался. Проверки: `.venv\Scripts\python.exe -m pip install -e ".[dev]"` — успешно; `.venv\Scripts\python.exe -m pytest` — `62 passed`; `.venv\Scripts\python.exe -m compileall -q app` — успешно.
- PR #23, ветка `codex/non-blocking-meeting-processing`: доработка этапа 8 после ручной проверки. Завершение встречи разделено на быструю остановку записи и фоновую обработку. После остановки записи `active_meeting` сбрасывается, кнопка `Начать встречу` снова доступна, а FFmpeg/Whisper/Summary по завершенным встречам выполняются в очереди. Этап 8 остается `На проверке`, этап 9 не начинался. Проверки: `.venv\Scripts\python.exe -m pytest` — `64 passed`; `.venv\Scripts\python.exe -m compileall -q app` — успешно.
- PR #24, ветка `codex/faster-whisper-transcription`: доработка этапа 8 для ускорения локальной транскрипции. Добавлен optional backend `faster_whisper`, настройка `transcription.backend`, readiness-проверка выбранного backend, fallback на прежний `whisper_cli` по умолчанию, README-инструкция и тесты. Аудио остается локальным и не отправляется во внешние сервисы. Этап 9 не начинался. Проверки: `.venv\Scripts\python.exe -m pip install -e .` — успешно после повторного последовательного запуска; `.venv\Scripts\python.exe -m pip install -e ".[dev]"` — успешно; `.venv\Scripts\python.exe -m pip install -e ".[faster-whisper]"` — успешно; `import faster_whisper` — `1.2.1`; `.venv\Scripts\python.exe -m pytest` — `71 passed`; `.venv\Scripts\python.exe -m compileall -q app` — успешно.
- PR #25, ветка `codex/ui-shell-redesign`: доработка этапа 8 с новым светлым UI-каркасом главного окна. Добавлены левая навигация `Рабочий день / Ревью / Архив / Настройки / Справка`, теплая Burger King-палитра, заголовки страниц и placeholder-разделы для будущих экранов без изменения OBS/FFmpeg/Whisper/Summary lifecycle. Этап 8 остается `На проверке`, этап 9 не начинался. Проверки: `.venv\Scripts\python.exe -m pytest` — `72 passed`; `.venv\Scripts\python.exe -m compileall -q app` — успешно.
- PR #26, ветка `codex/workday-screen-layout`: доработка этапа 8 с новой композицией экрана `Рабочий день`. Блоки главного сценария переставлены в порядок `Готовность системы`, `Состояние дня`, `Активный созвон`, `Встречи за день`, `Pipeline встречи`, `Действия`; добавлены краткие статусы активного созвона и количества встреч за день. Логика OBS, FFmpeg, Whisper/faster-whisper, Summary generation и lifecycle встреч не менялась. Этап 8 остается `На проверке`, этап 9 не начинался. Проверки: `.venv\Scripts\python.exe -m pytest` — `73 passed`; `.venv\Scripts\python.exe -m compileall -q app` — успешно.
- PR #27, ветка `codex/pipeline-status-layout`: доработка этапа 8 с новым отображением блока `Pipeline встречи`. Каждый шаг pipeline отображается отдельной компактной строкой с названием и статусом; существующие progress-события и metadata-логика не менялись. Логика OBS, FFmpeg, Whisper/faster-whisper, Summary generation и lifecycle встреч не менялась. Этап 8 остается `На проверке`, этап 9 не начинался. Проверки: `.venv\Scripts\python.exe -m pytest` — `74 passed`; `.venv\Scripts\python.exe -m compileall -q app` — успешно.
- PR #28, ветка `codex/prototype-card-polish`: доработка этапа 8 с визуальным приближением карточек главного экрана к прототипу. `Готовность системы` переведена в сетку readiness-карточек с короткими бейджами, `Папка данных` удалена из этого блока, кнопки `Проверить готовность` и `Свернуть` перенесены в header блока, основные действия получили primary/danger-стили. Логика OBS, FFmpeg, Whisper/faster-whisper, Summary generation и lifecycle встреч не менялась. Этап 8 остается `На проверке`, этап 9 не начинался. Проверки: `.venv\Scripts\python.exe -m pytest` — `75 passed`; `.venv\Scripts\python.exe -m compileall -q app` — успешно.
- Ветка `codex/workday-dashboard-prototype`: текущая доработка этапа 8 с приведением вкладки `Рабочий день` к прототипу. Действия перенесены в блоки `Состояние дня`, `Активный созвон` и `Встречи за день`; активный созвон получил таймер; встречи за день отображаются карточками-аккордеонами; pipeline показывается внутри раскрытой карточки конкретной встречи. `Проверить OBS` удаляется как отдельная кнопка главного экрана, потому что OBS проверяется через `Проверить готовность`. Функционал задач дня скрыт из UI, но старая файловая логика не удаляется. AI-агрегация итогов дня из всех встреч помечена как будущий функциональный PR. Этап 8 остается `На проверке`, этап 9 не начинался. Проверки: `.venv\Scripts\python.exe -m pytest` — `75 passed`; `.venv\Scripts\python.exe -m compileall -q app` — успешно.
- Ветка `codex/review-screen-prototype`: stacked-доработка этапа 8 с приведением вкладки `Ревью` к прототипу. Список встреч заменен карточками, выбранная встреча открывает вкладки `Итоги встречи` и `Транскрипт`, transcript отображается read-only, `Итоги дня` вынесены в отдельный блок и не создаются до появления `00_day_summary_draft.md`. Вкладка и UI задач дня скрыты, но старая файловая логика задач не удалялась. Проверки: `.venv\Scripts\python.exe -m pytest` — `76 passed`; `.venv\Scripts\python.exe -m compileall -q app` — успешно.
- Ветка `codex/settings-screen-prototype`: stacked-доработка этапа 8 с рабочей вкладкой `Настройки`. Добавлена форма редактирования локального `config.yaml`: storage, OBS WebSocket, transcription, summary и подготовленное поле темы. Пароль OBS отображается как password-field, API key напрямую не хранится — настраивается имя переменной окружения и `.env` файл. После сохранения приложение сообщает о необходимости перезапуска для применения сервисных настроек. Проверки: `.venv\Scripts\python.exe -m pytest` — `77 passed`; `.venv\Scripts\python.exe -m compileall -q app` — успешно.
- Ветка `codex/archive-help-ui-polish`: финальная stacked-доработка текущего UI-пакета этапа 8. `Архив` оформлен как честная заглушка будущего read-only просмотра прошлых дней без фиктивного функционала. `Справка` переведена в карточный формат с основным сценарием, local-first правилами и описанием OBS/FFmpeg/Whisper/Summary. Проверки: `.venv\Scripts\python.exe -m pytest` — `78 passed`; `.venv\Scripts\python.exe -m compileall -q app` — успешно.
- Ветка `codex/archive-help-ui-polish`: дополнительная UI-доводка вкладок `Рабочий день` и `Ревью` после визуальной проверки. В `Состоянии дня` оставлена одна динамическая кнопка старта/завершения рабочего дня; `Начать встречу` скрывается, пока рабочий день не активен; кнопки `Открыть папку встречи` и `Открыть папку дня` перенесены внутрь раскрытой карточки встречи; карточки встреч на вкладках `Рабочий день` и `Ревью` стали кликабельными почти всей областью; повторный клик по раскрытой встрече сворачивает ее; блок `Встречи за день` получил кнопку `Свернуть`/`Развернуть`; автоматический выбор встречи при старте и завершении созвона убран, чтобы экран не прыгал; карточки `Состояние дня` и `Активный созвон` больше не забирают свободную высоту страницы при сворачивании списка встреч и не сжимаются при неактивном рабочем дне; верхние карточки оформлены как парные внутренние панели с одинаковой высотой, статус дня и статус созвона вынесены в заголовки рядом с названиями блоков, видимый путь к папке дня убран из блока `Состояние дня`; окно `Начать встречу` заменено на внутреннюю модалку-overlay с затемнением фона, карточкой с тенью, названием встречи, статусом записи и проверкой пустого названия; встречи на вкладках `Рабочий день` и `Ревью` сортируются свежими сверху; pipeline раскрытой встречи отображается карточками этапов `OBS запись`, `Аудио`, `Транскрипция`, `Итоги` с отдельным бейджем статуса и описанием. Логика OBS, FFmpeg, Whisper/faster-whisper, Summary generation и lifecycle хранения не менялась. Этап 8 остается `На проверке`, этап 9 не начинался. Проверки: `.venv\Scripts\python.exe -m pytest` — `82 passed`; `.venv\Scripts\python.exe -m compileall -q app` — успешно.
- PR #33, ветка `codex/sync-main-with-ui-stack`: `main` синхронизирован с финальным UI-стеком PR #30–#32 после stacked-merge. Новая функциональность не добавлялась; логика OBS, FFmpeg, Whisper/faster-whisper и Summary generation не менялась. Проверки: `.venv\Scripts\python.exe -m pytest` — `82 passed`; `.venv\Scripts\python.exe -m compileall -q app` — успешно.
- PR #34, ветка `codex/day-summary-pipeline`: доработка этапа 8 с карточкой и pipeline `Итоги дня`. После завершения рабочего дня создается карточка `Итоги дня`, которая отображается первой в `Встречи за день` и первой в `Ревью`; дневной pipeline собирает summary встреч, явно помечает встречи без summary, генерирует `00_day_summary_draft.md` через OpenAI-compatible endpoint / ProxyAPI и формирует переходы к transcript встреч внутри приложения. Если обработка встреч еще идет, `Итоги дня` ожидают очередь; при повторном завершении дня без новых встреч черновик не пересобирается. Во внешний сервис для дневных итогов отправляются только тексты summary встреч, не аудио, не видео и не transcript. Этап 8 остается `На проверке`, этап 9 не начинался. Проверки: `.venv\Scripts\python.exe -m pytest` — `85 passed`; `.venv\Scripts\python.exe -m compileall -q app` — успешно.
- Ветка `codex/floating-meeting-control`: доработка этапа 8 с плавающим desktop control поверх окон Windows. Добавлен отдельный PySide6-виджет `FloatingMeetingControl`, который автоматически показывается при запуске, перетаскивается мышкой, скрывается/показывается из основного окна, открывает основное приложение, запускает существующий старт рабочего дня, стартует встречу через ввод названия, показывает счетчик длительности активного созвона и завершает активный созвон только после подтверждения. Floating control использует состояние и lifecycle основного окна, не создает отдельный `StorageService`, не дублирует OBS/FFmpeg/Whisper/Summary pipeline и не блокирует старт следующего созвона во время фоновой обработки предыдущей встречи. Этап 8 остается `На проверке`, этап 9 не начинался. Проверки: `.venv\Scripts\python.exe -m pip install -e ".[dev]"` — успешно; `.venv\Scripts\python.exe -m pytest` — `89 passed`; `.venv\Scripts\python.exe -m compileall -q app` — успешно.
- Ветка `codex/dark-theme-ui`: доработка этапа 8 с полноценной темной темой UI и защитой качества transcript. Добавлен `ui.theme` для основного окна и `ui.floating_theme` для floating control; настройки темы сохраняются в локальный `config.yaml`, основная тема применяется сразу после сохранения, floating control может наследовать тему приложения или использовать отдельную светлую/темную тему. Темная палитра применена к основному окну, карточкам, настройкам, ревью, модальному окну старта встречи и floating control; дополнительно темная тема покрывает страницы внутри scroll-area, промежутки между карточками и подписи форм в `Рабочий день` и `Настройки`. Для `faster-whisper` добавлен `vad_filter` по умолчанию, transcript получает `quality`/`quality_warnings`, подозрительный transcript не отправляется в OpenAI-compatible endpoint / ProxyAPI для summary, встреча получает понятный статус проверки, а раскрытая карточка встречи получила кнопку `Повторить обработку`. Логика OBS, FFmpeg, записи и локального хранения не менялась. Этап 8 остается `На проверке`, этап 9 не начинался. Проверки: `python -m pytest` — `95 passed`; `python -m compileall -q app` — успешно.
- Ветка `codex/summary-aitunnel-config`: точечная доработка summary-настроек после обсуждения стоимости внешних сервисов. AI Tunnel стал рекомендуемым OpenAI-compatible endpoint для генерации итогов: дефолтные настройки используют `AITUNNEL_KEY` и `https://api.aitunnel.ru/v1/`, README и `config.yaml.example` обновлены, readiness показывает AI Tunnel endpoint отдельным понятным статусом. ProxyAPI сохранен как альтернативный custom endpoint. Внешняя транскрипция через AI Tunnel не добавлялась. Этап 8 остается `На проверке`, этап 9 не начинался. Проверки: `python -m pytest` — `96 passed`; `python -m compileall -q app` — успешно.
- Ветка `codex/aitunnel-transcription-backend`: точечная доработка этапа 8 с optional backend `aitunnel` для внешней транскрипции через AI Tunnel STT. Добавлен `AITunnelTranscriber`, readiness-проверка ключа/endpoint для внешней транскрипции, поля в экране `Настройки`, README-инструкция и тесты. После ручной проверки настройки дополнены общим блоком `secrets.env_file`: API-ключ может храниться в одном локальном `.env`, readiness и runtime внешней транскрипции используют его для `AITUNNEL_KEY`. Настройки транскрипции разделены на backend-профили `whisper_cli`, `faster_whisper` и `aitunnel`, поэтому модель и параметры каждого backend сохраняются отдельно; `config.yaml.example` переведен на структуру `transcription.backends`. Экран `Настройки` показывает только поля, релевантные выбранному backend, а AI Tunnel получил выпадающий список моделей с ценами: `Whisper Large V3 Turbo — 0.13 ₽/мин`, `Whisper Large V3 — 0.36 ₽/мин`, `Whisper 1 — 1.15 ₽/мин`. Блок `Summary` упрощен по той же логике: скрыты поля API key, Base URL и `.env`, используется общий `secrets.env_file`, добавлен выпадающий список `GPT 5.4 Mini — 144 ₽/1M вход · 864 ₽/1M выход`, `GPT 5.4 Nano — 38.4 ₽/1M вход · 240 ₽/1M выход` и пункт `Другая модель AI Tunnel` для ручного ID модели. Исправлена ошибка ручной проверки: readiness мог показывать `API key найден`, но реальный Summary pipeline не видел общий `secrets.env_file`; теперь runtime Summary использует тот же fallback. После `Сохранить настройки` новая транскрипционная конфигурация применяется для следующих встреч сразу, если нет активной обработки; текущий pipeline при наличии фоновой обработки безопасно завершается со старой конфигурацией. По умолчанию остается локальный `whisper_cli`; `faster_whisper` сохраняется; во внешний сервис при `aitunnel` отправляется `audio.wav`, видео не отправляется. Chunking длинного аудио не реализован и оставлен будущей отдельной доработкой. Этап 8 остается `На проверке`, этап 9 не начинался. Проверки: `python -m pytest` — `112 passed`; `python -m compileall -q app` — успешно.
